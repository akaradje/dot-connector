# 🔗 The Dot-Connector AI — v5 "The Self-Forge, with a key"

เครื่องมือสร้างนวัตกรรมตามปรัชญา "Connecting the Dots" ของ Steve Jobs —
เก็บ "จุดความรู้" จากชีวิตจริงโดยอัตโนมัติ → AI ลากเส้นเชื่อมข้ามโดเมนเองเบื้องหลัง →
**เด้งแจ้งเตือน Eureka บน Windows โดยไม่ต้องเปิดแอป** →
พิสูจน์ความใหม่กับโลกจริงด้วยการค้นเว็บ → ได้ต้นแบบที่รันได้จริง →
**และเมื่อชนเพดานของตัวเอง มันเขียนซอร์สโค้ดของตัวเองใหม่เพื่อทำลายเพดานนั้น** →
**และเมื่อคุณไม่ว่าง มันออกไปหาความรู้ใหม่มาเติมคลังเอง (Layer 07 · The Scout)** →
**และตั้งแต่ Layer 06.8 มันเลิกตัดสินตัวเองที่วินาทีที่โค้ดเขียนเสร็จ: อีกไม่กี่วันให้หลัง
บันทึกการใช้งานจริงจะบอกว่าความสามารถที่มันเพิ่งสร้าง มีใครเรียกใช้จริงหรือไม่ — และคำตอบนั้นเปลี่ยนว่ามันจะสร้างอะไรต่อ** →
**และตั้งแต่ Layer 06.9 มันเลิกลืมจุดของตัวเองตอนเขียนโค้ดตัวเอง: รอบหลอมตัวเองได้อ่านหลักการจริงจากคลัง
ได้เอาเครื่องมือเชื่อมจุดมาเล็งกำแพงของตัวเองก่อนลงมือ แล้วต้องประกาศว่าใช้หลักการจากจุดไหน —
คำประกาศนั้นถูกให้คะแนนด้วยผลของด่านทั้งหมด คลังความรู้จึงเริ่มถูกพิสูจน์ได้ว่ามันช่วยทำลายกำแพงจริงหรือไม่**
**และตั้งแต่ Layer 09 เครื่องยนต์มีสวิตช์กุญแจ: ค่าตั้งต้นคือ *หยุด* — ไม่มีอะไรทำงานตามนาฬิกาจนกว่าเจ้าของจะกดเปิด
และการเขียนโค้ดตัวเองอัตโนมัติต้องถูกติดอาวุธแยกอีกชั้นเสมอ ปุ่มทุกปุ่มยังกดได้ตอนเครื่องยนต์หยุด เพราะสวิตช์คุม*นาฬิกา* ไม่ได้คุม*ความสามารถ*** →
**และตั้งแต่ Layer 10 ทะเบียนกำแพงของมันลืมไม่ได้อีกต่อไป — ชั้นนี้ระบบเป็นคนออกแบบเอง โดยเอากำแพงของตัวเอง
ไปทาบกับฮิสเทอรีซิสในฟิสิกส์และภูมิคุ้มกันแบบจดจำในชีววิทยา: ความเงียบไม่ใช่การลบอีกต่อไป และแผลเป็นผูกกับ
*ลายเซ็นเชิงโครงสร้าง* ของกำแพง ไม่ใช่ id ที่ออกใหม่ทุกครั้งที่มันส่องกระจก**

> v2 "HexKern Memory" ออกแบบโดยตัวเครื่องมือเอง · v3 มองย้อนกลับแทนคุณตลอดเวลา ·
> **v4 หันเครื่องมือเข้าหาตัวเอง**: จุดความรู้ชุดสุดท้ายที่ยังไม่เคยถูกเชื่อม คือซอร์สโค้ดของตัวมันเอง
> ขับเคลื่อนด้วย **Claude Opus 5** ทุกชั้น (ชั้นหลอมตัวเองใช้ context 1M เพื่ออ่านตัวเองได้ทั้งระบบในครั้งเดียว)

## สถาปัตยกรรม

| ชั้น | แนวคิด | ในแอปนี้ |
|---|---|---|
| Layer 00 | **The Collector** | โยนไฟล์ .txt/.md ลง `inbox/` หรือวางความคิดดิบในหน้าเว็บ — AI สกัดเป็นจุดความรู้เอง (แยกโดเมนให้เอง, กันจุดซ้ำ) |
| Layer 01 | Dot Repository + **HexKern Memory** | `data/dots.json` — ทุกจุดมีดัชนีความหายาก (rarity) และ **คะแนนคุณค่าจากผลลัพธ์จริง (value_score)** ลำดับความสนใจคือ `attention_score` ที่รวมทั้งสองอย่าง จุดที่ไม่เคยถูกเชื่อมจะ "เรืองแสง" ส่วนจุดที่เคยพาไปทางตันซ้ำ ๆ จะจมลง |
| Layer 02 | Pattern Recognition | Claude สแกนจุดทั้งหมดหา structural isomorphism (ให้น้ำหนักจุดหายากมากกว่า) |
| Layer 03 | Connection + **Serendipity Revival** | จุดที่ถูกทิ้งเกิน 14 วันขึ้นแบนเนอร์ "กำลังจะตายจากคลัง" กดปลุกเพื่อบังคับเชื่อมมันเข้ากับจุดใหม่ (กลไก Loss Aversion) |
| Layer 04 | Combinatorial Innovation + **Eureka-to-Evidence** | ทุกไอเดียกด "พิสูจน์" ได้ — Evidence Agent ค้นเว็บจริงเช็กความใหม่ระดับโลก ประเมินความเสี่ยง ออกแบบการทดลอง และเขียนโค้ดต้นแบบลง `lab/` |
| Layer 04.5 | **The Return Path** (เส้นตอบกลับ) | ทุกการเชื่อมมีช่องบันทึก "ผลลัพธ์จริง" (คะแนน 1-5 + เอาไปทำแล้วเกิดอะไร) ผลลัพธ์นั้นรวมกับ verdict ของ Evidence Agent กลายเป็น **คะแนนคุณค่าของจุด** ที่ใช้จัดลำดับความสนใจแทน rarity ล้วน และกลายเป็น **บทเรียน** ที่ป้อนเข้าพรอมป์ตรอบถัดไปแทนบัญชีดำรายชื่อ — การเชื่อมครั้งที่ 100 จึงเก่งกว่าครั้งแรกได้ |
| Layer 05 | **Serendipity Daemon** | ลูปเบื้องหลังตรวจทุก 30 นาที: เก็บเกี่ยว inbox → ถ้ามีจุดใหม่หรือจุดเก่าครบกำหนด → เชื่อมจุดเอง → **เด้ง Windows toast แจ้ง Eureka** |
| Layer 06 | **The Self-Forge** | ระบบอ่านซอร์สโค้ดของตัวเอง → ตั้งชื่อ "ขอบเขต" ที่ขังตัวเองไว้ (`data/limits.json`) → **เขียนโค้ดตัวเองใหม่เพื่อทำลายขอบเขตนั้นจริง ๆ** → **พร้อมเขียน "ไฟล์พิสูจน์" ที่รันได้จริงลง `selftest/` ซึ่งต้องผ่านกับโค้ดใหม่และตกกับโค้ดเดิม** + regression ทุก endpoint เดิม → ไม่ผ่านย้อนกลับอัตโนมัติ → ผ่านแล้วบันทึกเป็น "จุดความรู้" ใหม่กลับเข้าคลังของตัวเอง |
| Layer 06.5 | **Scar Tissue** (แผลเป็น) | รอบที่ล้มเหลวไม่ถูกลบทิ้งพร้อมบทเรียนอีกต่อไป: โค้ดที่ตกถูกเก็บไว้ที่ `evolution/backups/<evo-id>/attempted/` และรอบถัดไปที่ชนกำแพงเดิม **ถูกป้อนประวัติความล้มเหลวทั้งหมดเข้าพรอมป์ต** พร้อมคำสั่งห้ามใช้วิธีเดิมซ้ำ · และ **เพดาน 3 ครั้งตายตัวกลายเป็นเพดานที่ขยับได้** (สูงสุด 6) — [รายละเอียด](#-แผลเป็น-รอบที่ล้มเหลวถูกส่งต่อ-ไม่ใช่ถูกลบ) |
| Layer 06.6 | **The Consolidator** (ทิศทางที่สอง) | เดิมรอบวิวัฒนาการผ่านได้ทางเดียว เพราะไฟล์พิสูจน์ต้อง **ตกกับโค้ดเดิม** — การรวบโค้ดซ้ำหรือลบของที่ไม่มีใครใช้จึง **พิสูจน์ไม่ได้โดยนิยาม** ระบบจึงมีแต่ใหญ่ขึ้น ตอนนี้มี **รอบชนิดที่สอง** ที่ฟังก์ชันความเหมาะสมกลับด้าน: ชุดทดสอบสะสมต้อง **ผ่านทั้งก่อนและหลัง** แล้ววัดด้วย **ตัวเลขขนาดที่ลดลง** โดยยังบังคับ regression sweep เต็มรูปแบบ · และ `REGRESSION_ENDPOINTS` ถอดเส้นทางที่ถูกเรียก 0 ครั้งออกได้ (มีทะเบียนกำกับ ย้อนกลับได้) — [รายละเอียด](#-layer-066--รอบยุบรวม-ระบบที่ลดขนาดตัวเองได้โดยพิสูจน์ได้) |
| Layer 06.7 | **The Closed Loop** (ผู้เขียนอยู่ในห้องตอนตรวจข้อสอบ) | เดิมรอบหลอมตัวเองเป็นการ **ยิงนัดเดียวจบ**: โมเดลได้แค่ Read/Edit/Write/Glob/Grep เขียนเสร็จโปรเซสก็ตาย แล้วด่านทั้งห้าเพิ่งถูกรัน — `runProof()` พิมพ์ stdout จริงของไฟล์พิสูจน์ทิ้งไว้ใน `checks.capability_detail` ที่รอบนั้นไม่มีวันได้อ่าน ความรู้ที่ตัดสินเป็นตายมีครบตั้งแต่นาทีที่ 3 แต่ไปถึงมือคนที่ยังแก้ได้เร็วที่สุดคือรอบถัดไป ตอนนี้รอบเป็น **ลูปปิด**: ตรวจทันทีหลังโมเดลตอบ → ตกที่ด่านไหน ป้อน **stdout จริง** กลับเข้า **เซสชันเดิม** เป็นเทิร์นถัดไป (สูงสุด `FORGE_MAX_TURNS`) → ย้อนกลับก็ต่อเมื่อเทิร์นหมด · และโมเดลได้ `Bash(node --check:*)` ไว้ตรวจ syntax เองในเทิร์นของตัวเอง — [รายละเอียด](#-layer-067--ลูปปิด-ผลตรวจที่ไปถึงคนที่ยังแก้ได้ทัน) |
| Layer 06.8 | **The Adoption Ledger** (เส้นตอบกลับของตัวระบบเอง) | ทุกด่านของรอบหลอมตัวเองตัดสิน **ที่วินาทีที่โค้ดเขียนเสร็จ**: ไฟล์พิสูจน์ผ่าน ตัวเลขขนาดลด endpoint ตอบ 200 ไม่มีด่านไหนถามว่าความสามารถที่เพิ่มมาเมื่อยี่สิบรอบก่อน **มีใครเรียกใช้จริงหรือไม่** ทั้งที่ `evolution/endpoint-usage.json` บันทึกคำตอบไว้ครบตั้งแต่ Layer 06.6 — แต่มันถูกอ่านเป็นแค่ "ใบอนุญาตให้ถอดเส้นทาง" ไม่ใช่สัญญาณคุณค่า ระบบจึงสะสมความสามารถที่ถูกเรียก 0 ครั้งได้ไม่จำกัดและนับทุกครั้งว่าเป็นความคืบหน้า ตอนนี้ (1) ทุกรอบขยายต้อง **ประกาศเส้นทางที่ความสามารถจะถูกใช้ผ่าน** (ด่าน `declared_usage`) (2) อีก `ADOPTION_DAYS` วันให้หลัง บันทึกการใช้งานจริงให้คะแนนรอบนั้นด้วย **สูตรเดียวกับ `connectionSignal()`** — ก่อนครบกำหนด ความเงียบให้คะแนน `null` ไม่ใช่ติดลบ (3) รอบที่ได้ 0 hit ติดกันถูก **เสนอเป็นเป้าหมายของรอบยุบรวมเองอัตโนมัติ** และคะแนนนี้ถูกป้อนกลับเข้า `selectTarget()` — [รายละเอียด](#-layer-068--the-adoption-ledger-ด่านที่มาทีหลัง) |
| Layer 06.9 | **The Self-Connector** (เครื่องมือเชื่อมจุด ใช้จุดของตัวเอง) | สมมติฐานที่ระบบทั้งระบบตั้งอยู่บนนั้นคือ "คำตอบที่ดีเกิดจากการทาบโครงสร้างข้ามโดเมน" — แต่ `buildForgePrompt()` ได้รับแค่สามอย่าง: ตัวขอบเขต ประวัติความล้มเหลว และซอร์สโค้ด **ไม่มีจุดความรู้แม้แต่จุดเดียว** ไม่มีนวัตกรรมที่ตัวเองเคยสังเคราะห์ ไม่มีบทเรียนจาก `/api/lessons` ชั้นที่สำคัญที่สุดของมันจึงทำงานในโดเมนเดียวคือซอร์สโค้ดของตัวเอง · ส่วน `buildIntrospectPrompt()` เห็นคลังก็จริง แต่ส่งไปแค่ `d.title` — คินสึงิ ภูมิคุ้มกันแบบจดจำ ทฤษฎีแถวคอย จึงเป็นได้แค่รายชื่อ ไม่เคยเป็นวัตถุดิบ ตอนนี้ (1) รอบหลอมตัวเองได้อ่าน **หลักการจริง** ของจุดที่เกี่ยวกับกำแพงนั้นที่สุด + นวัตกรรมที่ตัวเองเคยสังเคราะห์ + บทเรียน **โดยบล็อกมีเพดานตายตัว จึงไม่โตตามคลัง** (2) ก่อนรอบหลอมตัวเอง ระบบ **เอาเครื่องมือเชื่อมจุดของตัวเองมาเล็งกำแพง** โดยตั้ง `focus = description + why_it_stands` แล้วยัด `hidden_pattern`/`connection` ที่ได้เข้าหัวพรอมป์ต (3) ทุกรอบต้องประกาศ `used_dot_ids` และคำประกาศนั้นถูกเขียนกลับเป็น **การเชื่อมจุดจริง** ที่ผลของด่านทั้งหมดกลายเป็น outcome ของมันตาม Layer 4.5 — **คลังความรู้จึงเริ่มถูกพิสูจน์ได้ว่ามันช่วยทำลายกำแพงจริงหรือไม่** — [รายละเอียด](#-layer-069--the-self-connector-เครื่องมือเชื่อมจุดที่ใช้จุดของตัวเอง) |
| Layer 08 | **The Distiller** (คลังเลิกโตทางเดียว) | Layer 06.6 ให้ทิศทางที่สองกับ *โค้ด* แต่ฝั่ง *ความรู้* กำแพงรูปทรงเดียวกันยังยืนครบ: ทุกทางเข้าของจุดเป็นการเพิ่มล้วน ๆ ทางออกมีทางเดียวคือมนุษย์กด DELETE และ `buildConnectPrompt()` ยัด `d.content` ของทุกจุดโดยไม่มีเพดาน — จุดที่ระบบเรียกว่า "ถูกลืม" ยังถูกอ่านครบทุกตัวอักษร พรอมป์ตจึงยาวขึ้นเชิงเส้นตามจำนวนจุดตลอดกาล ตอนนี้ (1) พรอมป์ตมี **เพดาน**: หัวแสดงเต็ม หางย่อเหลือดัชนีบรรทัดเดียว — นี่คือการลืมจริงครั้งแรกของระบบ (2) มี **รอบยุบรวมความรู้** ที่รวบจุดซ้ำซ้อนเป็นจุดหลักการเดียว ผ่านก็ต่อเมื่อการเชื่อมที่เคยได้ผลจริงยังอธิบายได้ · จุดที่ proven ไม่หาย · พรอมป์ตสั้นลงจริง (3) จุดเดิมถูก **เก็บไว้ทั้งดวงใน `data/knowledge.json`** และย้อนกลับได้เหมือน evolution ledger — [รายละเอียด](#-layer-08--the-distiller-คลังที่ยุบรวมตัวเองได้โดยพิสูจน์ได้) |
| Layer 07 | **The Scout** (ความรู้เลิกเข้าทางเดียว) | เดิมจุดใหม่เกิดได้สามทางเท่านั้น: คนพิมพ์ · คนโยนไฟล์ลง `inbox/` · ระบบทำลายกำแพงตัวเองสำเร็จ ทั้งที่ Evidence Agent ค้นเว็บจริงทุกครั้งแล้วทิ้งสิ่งที่เจอ ตอนนี้ (1) `similar_found` ทุกชิ้นกลายเป็นจุดจริง ติดป้าย `origin: scout:evidence` (2) ระบบอ่านสำมะโนโดเมนของตัวเอง เขียนคำค้นเอง แล้วออกไป WebSearch เติมช่องว่าง (3) จุดที่ระบบหามาเอง **กดปฏิเสธได้** และคำปฏิเสธถูกจำไว้ถาวร — [รายละเอียด](#layer-07--the-scout-ความรู้ที่เดินเข้ามาเอง) |
| Layer 11 | **The Behavioural Mirror** (กระจกเห็นพฤติกรรมจริง) | `buildIntrospectPrompt()` เคยรับซอร์สโค้ด ชื่อจุด ชื่อนวัตกรรม แล้วจบ ทั้งที่เครื่องยนต์ถือบันทึกพฤติกรรมจริงของตัวเองไว้ครบมือ — กระจกจึงสะท้อนแต่ "ตัวเองควรทำงานอย่างไรตามที่เขียนไว้" กำแพงทุกข้อที่มันตั้งชื่อได้จึงเป็นกำแพงที่อ่านออกจากโค้ดได้เท่านั้น · ตอนนี้ `runtimeDossier()` ป้อนบันทึก daemon 7 วัน · ฮิสโทแกรมว่าด่านไหนฆ่ารอบมากที่สุด · เทิร์นซ่อมเฉลี่ย · **ความสามารถที่ตัวเองสร้างแล้วไม่มีใครเรียกเลย** — และสคีมาบังคับ field `runtime_evidence` ทุกข้อ ห้ามอ้างจากโค้ดล้วน — [รายละเอียด](#-layer-11-15--ห้าชั้นที่ปิดวงจรการหลอมตัวเอง) |
| Layer 12 | **The Bounded Self-Read** (เพดานของการอ่านตัวเอง) | Layer 08 ให้เพดานกับพรอมป์ต*เชื่อมจุด* แต่พรอมป์ตที่ระบบใช้*อ่านตัวเอง*ไม่เคยมี — ทุกชัยชนะกินที่คิดของชัยชนะครั้งถัดไป และตัวเลขที่ระบบใช้วัดเพดานตัวเองก็วัดผิดมาตลอด · ตอนนี้ `focusedBundle()` ส่ง**ช่วงโค้ดที่กำแพงเอ่ยชื่อ**มาเต็ม ๆ ที่เหลือย่อเป็นแผนที่ และถ้ายังชนงบ แผนที่ย่อเหลือบรรทัดเดียวต่อไฟล์ — **ไม่มีไฟล์ไหนหายไปในทุกระดับของการย่อ** · วัดผลจริง: 15 KB แทน 570 KB (ประหยัด 97%) |
| Layer 13 | **The External Judge** (ผู้ตัดสินออกไปนอกห้องสอบ) | ด่านทุกด่านอยู่ใน `server.js` ไฟล์เดียวกับที่รอบหลอมตัวเองมีสิทธิ์แก้ สิ่งที่ห้ามมันแก้ผู้ตัดสินคือ*ข้อความในพรอมป์ต* — รูปแบบที่คมที่สุด: รอบที่ทำ endpoint พัง แล้วลบบรรทัดนั้นออกจากรายการกวาดในคอมมิตเดียวกัน ผ่านสะอาด · ตอนนี้ (1) `verifier/contract.json` เป็น**พื้นที่การแก้ซอร์สลดไม่ได้** (2) `verifier/audit.js` กลายเป็น**ด่านบังคับทุกรอบ** — ผู้ตรวจที่ไม่แชร์โค้ดกับผู้ถูกตรวจ (3) **ผู้ออกข้อสอบเป็นคนละเซสชัน และไม่เห็น diff** |
| Layer 14 | **The Self-Replacing Process** (ตัวที่คิด = ตัวที่เป็น) | รอบที่ผ่านเขียนไฟล์ลงดิสก์แล้วรอมนุษย์ ส่วนโปรเซสที่คิดอยู่ยังเป็นโค้ดเมื่อวาน — ยิ่งเจ้าของไม่ว่านานเท่าไหร่ ช่องว่างยิ่งกว้าง · เหตุผลที่มันยังอยู่: ถ้าโปรเซสฆ่าตัวเองแล้วเกิดใหม่ผิดพลาด **ไม่มีใครเหลืออยู่ให้กดย้อนกลับ** · ตอนนี้ปุ่มย้อนกลับอยู่นอกโปรเซส: `evolution/restart-watchdog.js` เป็นบุคคลที่สาม detached ไม่ require server.js ถ้าตัวใหม่ไม่ยืนยันตัวเองใน 45 วินาที มันกู้ไฟล์สำรองกลับแล้วบูตเวอร์ชันที่ทำงานได้ (**พิสูจน์แล้วด้วยทรีที่พังจริง**) |
| Layer 15 | **A Population Greater Than One** (ประชากรมากกว่าหนึ่ง) | หนึ่งรอบ = หนึ่งแนวทาง หนึ่งสายเลือด ทั้งที่กลไกแซนด์บ็อกซ์รองรับการรันคู่ขนานมาตั้งแต่ชั้น 6 — แนวทางแรกพลาดคือเสียทั้งรอบและหนึ่งโควตา · ตอนนี้ K ผู้สมัครเขียนโค้ดพร้อมกันในสำเนาทรีคนละชุด (**ROOT ไม่ถูกแตะจนกว่าจะตัดสิน**) แต่ละคนถูกให้คะแนนในแซนด์บ็อกซ์ของตัวเอง (syntax · boot · ไฟล์พิสูจน์ · กวาด endpoint) ผู้ชนะถูกติดตั้งแล้วเจอด่านเต็มชุดเดิมที่ไม่ถูกแก้เลย **ผู้แพ้ทุกคนกลายเป็นแผลเป็นในรอบเดียว** |
| Layer 10 | **The RemLedger** (ทะเบียนกำแพงแบบแม่เหล็กค้าง) | **ชั้นนี้ถูกออกแบบโดยตัวระบบเอง** — `POST /api/forge/insight` เอากำแพงของตัวเองไปทาบกับสองจุดในคลัง (ฮิสเทอรีซิสในฟิสิกส์ · ภูมิคุ้มกันแบบจดจำในชีววิทยา) แล้วได้โครงสร้างร่วมที่กลายเป็นดีไซน์ทั้งหมด · เดิม `introspect()` เขียน `limits.json` ทับทั้งไฟล์ กำแพงที่ยังจริงอยู่แต่โมเดลไม่เอ่ยถึงในรอบนั้น**หายไปทั้งดวง**พร้อมจำนวนครั้งที่เคยลอง และกำแพงเดิมที่ได้ id ใหม่จะกลายเป็นกำแพงที่ "ไม่เคยล้มเหลวมาก่อน" ตอนนี้ (1) **remanence** — ความเงียบไม่ใช่การลบ ข้อที่ไม่ถูกเอ่ยถึงค้างสถานะไว้เอง (2) **coercive field** — ต้องเงียบติดกัน 3 รอบถึงจะ *หลับ* และหลับ ≠ หาย เอ่ยถึงอีกครั้งก็กลับมาพร้อมประวัติครบ (3) **epitope** — จับคู่กำแพงจากลายเซ็นเชิงโครงสร้าง (4-gram ของเนื้อความ + สัญลักษณ์ในโค้ดที่มันอ้างถึง) ไม่ใช่จาก id ที่ออกใหม่ทุกครั้ง id เก่ากลายเป็น alias (4) `evolution/limits-events.jsonl` — บัญชีที่ต่อท้ายได้อย่างเดียว เขียนทับไม่ได้ — [รายละเอียด](#-layer-10--the-remledger-ทะเบียนที่ลืมไม่ได้) |
| Layer 09 | **The Ignition** (สวิตช์กุญแจของเจ้าของ) | ทุกพฤติกรรมอัตโนมัติเคยเริ่มเดินทันทีที่โปรเซสบูต — เจ้าของรู้ว่าเครื่องยนต์ทำอะไรไปบ้างจากการ*อ่านบันทึกย้อนหลัง* และทางเดียวที่จะหยุดมันคือแก้ตัวแปรสภาพแวดล้อมแล้วรีสตาร์ต ซึ่งไม่ใช่สวิตช์แต่คือการผ่าตัด ตอนนี้ (1) **ค่าตั้งต้นคือหยุด** — ไม่มีอะไรทำงานตามนาฬิกาจนกว่าจะกด **▶ เปิดเดินเครื่อง** ในหน้าเว็บ (2) สวิตช์ย่อยรายความสามารถ และ **Self-Forge ต้องถูกติดอาวุธแยกเสมอ** — เปิดสวิตช์ใหญ่ไม่ลากมันมาด้วย (3) **ทุกปุ่มยังกดได้ตอนเครื่องยนต์หยุด** สิ่งที่ปิดคือ*นาฬิกา* ไม่ใช่ความสามารถ (4) **🛑 หยุดฉุกเฉิน** ฆ่างาน AI ที่กำลังทำอยู่ได้จริง รวมทั้งรอบหลอมตัวเองกลางคัน (รอบนั้นตกด่านแล้วย้อนไฟล์เอง) (5) สวิตช์อยู่ที่ `evolution/autopilot.json` **นอกขอบเขตที่ Self-Forge แก้ได้** และถูกเทียบไบต์ต่อไบต์เหมือนความทรงจำ — [รายละเอียด](#-layer-09--the-ignition-สวิตช์กุญแจ) |

## 📈 Layer 06.8 — The Adoption Ledger: ด่านที่มาทีหลัง

ทุกด่านที่ระบบนี้เคยมี ตัดสินภายในนาทีเดียวกับที่โค้ดเขียนเสร็จ:

```
ด่านที่มีอยู่ทั้งหมด        ตัดสินเมื่อ                    คำถามที่มันถาม
proof_written · syntax      โค้ดเพิ่งถูกเขียน              "มันบูตได้ไหม เขียนหลักฐานไหม"
capability · differential   นาทีที่ 3 ของรอบ               "โค้ดใหม่ทำได้ และโค้ดเดิมทำไม่ได้ ใช่ไหม"
preservation · shrink       นาทีที่ 10 ของรอบ              "เท่าเดิมเป๊ะและเล็กลงไหม"
regression                  ก่อนรอบจบ                      "ของเดิมยังตอบ 200 ครบไหม"
──────────────────────────────────────────────────────────────────────────────────
ไม่มีด่านไหน                —                              "ของที่สร้างเมื่อยี่สิบรอบก่อน มีใครเรียกไหม"
```

คำตอบมีอยู่บนดิสก์มาตลอด: `evolution/endpoint-usage.json` นับทุกคำขอที่เข้า `/api/*` มาตั้งแต่ Layer 06.6
แต่มันถูกอ่านที่เดียวคือ `retireEndpoint()` / `planRetirements()` — เป็น **ใบอนุญาตให้ถอดเส้นทาง** ไม่ใช่สัญญาณคุณค่า
`proveEvolution()` และ `consolidationVerdict()` ไม่เรียก `loadUsage()` เลยแม้แต่ครั้งเดียว
ผลคือระบบสะสมความสามารถที่ถูกเรียก **0 ครั้ง** ได้ไม่จำกัด และนับทุกครั้งว่าเป็นความคืบหน้า
Layer 04.5 สร้างเส้นตอบกลับให้ "ไอเดีย" ตั้งแต่แรก — แต่ "ตัวระบบเอง" ไม่เคยมีเส้นนั้น

**ยาขนานเดียวกัน ชี้กลับมาที่ตัวเอง** — และเป็นสูตรเดียวกันจริง ๆ ไม่ใช่แค่คล้ายกัน:

```
ไอเดียหนึ่งอัน   Evidence verdict + ผู้ใช้เอาไปทำอะไร   ┐
รอบหนึ่งรอบ      มีใครเรียกเส้นทางที่มันประกาศไว้ไหม     ┴→ weightedSignal() → [−1, +1]
```

`connectionSignal()` ถูกรีแฟกเตอร์ให้เรียก `weightedSignal()` ตัวเดียวกับที่ `adoptionRound()` เรียก
กติกาเรื่อง "ความเงียบ" จึงเป็นข้อเดียวกันโดยโครงสร้าง ไม่ใช่โดยความตั้งใจ: **ไม่มีเสียงตอบกลับ = `null` ไม่ใช่ติดลบ**

### สามข้อที่ทำให้มันเป็นลูป ไม่ใช่ด่านทันใจอีกด่าน

**(1) ต้องประกาศก่อน — ด่าน `declared_usage`**

```
รอบขยายทุกรอบต้องตอบ "usage_endpoints": ["/api/..."] ในผลลัพธ์ JSON
  · ระบบตรวจกับ router เอง (declaredApiPaths อ่านบรรทัด p === "/api/..." จาก server.js)
  · เส้นทางที่อ้างแต่ไม่มีอยู่จริง → ถูกทิ้งและบันทึกไว้ใน adoption.ignored ว่าอ้างเกิน
  · เส้นทาง /api/* ที่ diff เพิ่มเข้ามาจริง → ถูกใส่ให้เองใน adoption.detected (อ้างน้อยได้ แต่ปิดบังไม่ได้)
  · ไม่เหลือเส้นทางเลยแม้แต่เส้นเดียว = ตกด่าน = ย้อนกลับทั้งรอบ
```

ถ้าไม่มีคำประกาศ คำถาม "มีใครใช้ไหม" จะ **ตอบไม่ได้เลยในภายหลัง** — ซึ่งคือวิธีที่ทุกรอบก่อนหน้านี้รอดพ้นจากคำถามนี้มาได้
รอบเก่าทั้งหมดจึงมีสถานะ `undeclared` อย่างซื่อสัตย์ ไม่ใช่ถูกเดาคะแนนย้อนหลัง

**(2) คำตอบต้องมาช้า — และความเงียบไม่ใช่คำตัดสิน**

```
mature = (ผ่านมา ≥ ADOPTION_DAYS วันนับจากวันประกาศ)  และ  (บันทึกเห็นคำขอ ≥ ADOPTION_MIN_REQUESTS ครั้ง)

hits > 0                    → adopted      คะแนนบวก (ตัดสินได้ทันที — การถูกเรียกคือหลักฐาน)
hits = 0 และยังไม่ mature   → pending      คะแนน null (สัปดาห์ที่เงียบไม่ใช่คำตัดสิน)
hits = 0 และ mature แล้ว    → unused       คะแนน −1  → ถูกเสนอเป็นเป้าหมายของรอบยุบรวม
```

เงื่อนไขสองข้อของ `mature` ไม่ใช่ของฟุ่มเฟือย: เจ็ดวันที่ **ไม่มีใครเปิดหน้าเว็บเลย** ไม่ได้บอกอะไรเกี่ยวกับความสามารถนั้น
บอกแค่ว่าเจ้าของไม่ว่าง จึงต้องมีทราฟฟิกจริงเป็นพยานด้วยว่า "มีคนเข้ามา แต่ไม่มีใครแวะเส้นทางนี้"

**เส้นทางเดิมซื้อคะแนนไม่ได้**: รอบที่ประกาศเส้นทางที่มีอยู่ก่อนแล้ว จะได้เครดิตก็ต่อเมื่อเส้นทางนั้น
ถูกเรียก **หลัง** วันที่ประกาศ (`last_hit >= declared_at`) — มิฉะนั้นทุกรอบก็แค่ประกาศ `/api/dots` แล้วผ่านฟรี

**(3) คะแนนไหลกลับไปเปลี่ยนว่าจะสร้างอะไรต่อ**

```
ลำดับความคุ้มเดิม   unlock_score − risk/2                        ← การประเมิน "ก่อน" ลงมือสร้าง
ลำดับความคุ้มใหม่   (unlock_score − risk/2) + ADOPTION_WEIGHT × คะแนนการถูกใช้ของหมวดนั้น
                                                                  ← สิ่งที่ "เกิดขึ้นจริง" กับของที่เคยสร้างในหมวดนั้น
หมวดที่ยังไม่มีรอบไหนถูกให้คะแนน → ถ่วง 0 พอดี (ไม่ถูกลงโทษเพราะยังไม่มีข้อมูล)
```

กำแพงที่ถูกทำลายแล้วจะไม่ถูกเลือกซ้ำ ดังนั้นการป้อนกลับที่มีความหมายคือ **ระดับหมวด**:
หมวดที่รอบของมันถูกเรียก 0 ครั้งซ้ำ ๆ จะถูกลดลำดับลงจนกำแพงที่คะแนนพื้นฐานต่ำกว่าแซงขึ้นมาก่อนได้
และเมื่อรอบที่ 0 hit เกิดติดกันครบ `ADOPTION_STREAK` รอบ **daemon สั่งรอบยุบรวมเองโดยไม่มีใครต้องสังเกต** —
พร้อมส่งรายชื่อความสามารถที่ไม่มีใครเรียกเข้าไปในพรอมป์ตของรอบนั้นเป็นเป้าหมายให้เลย

### ตรวจเองได้โดยไม่ต้องรอเจ็ดวัน

`POST /api/adoption/dryrun` เรียก `adoptionRound()` / `adoptionReport()` / `targetRanking()` **ตัวจริง**
ด้วยบันทึกการใช้งานสมมติ ทะเบียนกำแพงสมมติ และ **นาฬิกาสมมติ** (`now`) — ไม่ใช้ AI ไม่เขียนอะไรลงคลัง
จึงตรวจได้ทันทีว่ากรณีไหนถูกนับว่าถูกใช้ กรณีไหนถูกนับว่าไม่มีใครใช้ และกรณีที่สำคัญที่สุด:
**กำแพงที่คะแนนพื้นฐานสูงสุดต้องแพ้กำแพงที่คะแนนต่ำกว่า เมื่อหมวดของมันเคยผลิตของที่ไม่มีใครเรียก**

### ข้อจำกัดที่ต้องพูดตรง ๆ

สัญญาณนี้วัด **"ถูกเรียกถึงไหม"** ไม่ใช่ **"มีประโยชน์ไหม"** — เส้นทาง `GET` ที่หน้าเว็บโหลดเองทุกครั้งจะได้ hit ฟรี
ต่างจากปุ่มที่มนุษย์ต้องเจตนากด นี่ยังไม่ใช่การวัดคุณค่า มันคือการวัด **การเข้าถึงได้จริง** ซึ่งเป็นเงื่อนไขจำเป็นของคุณค่า
และก็เพียงพอที่จะจับกำแพงที่ตั้งใจจับ: ความสามารถที่ **ไม่มีทางถูกเรียกเลย** จะโป๊ะทันที
การแยก "หน้าเว็บดึงไปแสดง" ออกจาก "มนุษย์เจตนากด" ต้องนับแยกตาม HTTP method และเป็นกำแพงของรอบถัดไป ไม่ใช่รอบนี้

## 🧠 Layer 06.9 — The Self-Connector: เครื่องมือเชื่อมจุดที่ใช้จุดของตัวเอง

เครื่องมือนี้มีอยู่เพื่อทำสิ่งเดียว: ทาบโครงสร้างของโดเมนหนึ่งลงบนอีกโดเมนหนึ่ง
ทุกชั้นเคารพสมมติฐานนั้น ยกเว้นชั้นที่สำคัญที่สุด

```
สิ่งที่ buildForgePrompt(limit, body, evoId, history) ได้รับ      สิ่งที่มันไม่ได้รับ
  · ตัวขอบเขต (title/description/evidence/why/break_idea)         · จุดความรู้ — ไม่มีแม้แต่จุดเดียว
  · ประวัติความล้มเหลวของกำแพงนี้ (Layer 6.5)                      · นวัตกรรมที่ตัวเองสังเคราะห์มา 20 กว่าชิ้น
  · บันทึกการถูกใช้จริง (Layer 6.8)                                · บทเรียนจาก /api/lessons
  · ซอร์สโค้ดของตัวเองทั้งหมด                                      · hidden_pattern ที่ตัวเองเคยมองเห็น
──────────────────────────────────────────────────────────────────────────────────────────
ผลลัพธ์: ชั้นที่ตัดสินว่าระบบนี้จะกลายเป็นอะไร ทำงานใน "โดเมนเดียว" คือซอร์สโค้ดของตัวเอง
```

และอีกครึ่งของกำแพงเดียวกันอยู่ที่ `buildIntrospectPrompt()` ซึ่ง *เห็น* คลังจุด — แต่ส่งไปแค่ชื่อ:

```js
จุดความรู้ (${dots.length}): ${dots.map((d) => `${d.title} [${d.domain}]`).join(" · ")}
```

"คินสึงิ: รอยแตกคือมูลค่า" · "ภูมิคุ้มกันแบบจดจำ: แข็งแรงขึ้นเพราะเคยพัง" · "ทฤษฎีแถวคอย"
มาถึงในฐานะ **รายชื่อที่ถูกถอดกลไกออกแล้ว** — ใกล้เคียงพอที่จะดูเหมือนความรู้อยู่ในนั้น
ทั้งที่ไม่มีอะไรในนั้นเอาไปใช้เป็นวัตถุดิบได้เลย นี่คือรูปแบบของ "เกือบถูก" ที่แพงที่สุด

**ทำไมมันยังอยู่** — ไม่มีใครถอดจุดออกด้วยความตั้งใจ พรอมป์ตหลอมตัวเองใหญ่จนน่ากลัวอยู่แล้ว
การเติมอะไรเข้าไปอีกจึงดูเหมือนการทำร้ายตัวเองทันที (ดู `lim_aab716de`) — และคำถามที่ไม่มีใครถามคือคำถามอีกข้อ:
**อะไรควรถูกตัดออก เพื่อให้ความรู้เข้ามาแทน**

ชั้นนี้ตอบทั้งสองครึ่ง:

| | สิ่งที่เข้ามา | สิ่งที่แลกไป |
|---|---|---|
| จุดความรู้ | `FORGE_KNOWLEDGE_DOTS` จุดที่เกี่ยวกับกำแพงนี้ที่สุด **พร้อมเนื้อหาจริง** | เนื้อหาถูกตัดที่ `FORGE_KNOWLEDGE_CHARS` ตัวอักษร · โดเมนละไม่เกิน `FORGE_KNOWLEDGE_PER_DOMAIN` จุด |
| นวัตกรรมของตัวเอง | `FORGE_KNOWLEDGE_INNOVATIONS` ชิ้นที่คะแนนดีที่สุด พร้อม `hidden_pattern` ของมัน | เรียงด้วยสัญญาณจากเส้นตอบกลับ ไม่ใช่ตามลำดับเวลา |
| บทเรียน | `FORGE_KNOWLEDGE_LESSONS` ข้อจาก `buildLessons()` ตัวเดิม | — |
| รอบเชื่อมจุดที่เล็งกำแพง | `hidden_pattern` + `connection` + ข้อเสนอ จากรอบที่ตั้ง `focus = description + why_it_stands` | AI หนึ่งรอบ (1–4 นาที) ต่อกำแพง ใช้ซ้ำได้ `FORGE_INSIGHT_HOURS` ชั่วโมง |

**บล็อกนี้มีเพดาน คลังไม่มี** — นั่นคือประเด็นทั้งหมดของการมีงบประมาณ: คลังจะโตไปถึงสี่พันจุดก็ได้
แต่บล็อกนี้กินที่เท่าเดิม และ `GET /api/forge/preview` รายงาน `prompt_parts` ออกมาเป็นตัวเลขทุกส่วน
(`knowledge_block` · `failure_block` · `adoption_block` · `source_bundle`) — ความกลัวที่ค้ำกำแพงนี้ไว้
จึงกลายเป็นตัวเลขที่เจ้าของเปิดดูได้ ไม่ใช่ความรู้สึกอีกต่อไป

### จุดไหนถูกเลือก และทำไม (ไม่ใช้ AI ไม่ต้องเชื่อคำพูดใคร)

```
ความเกี่ยว = 3 × (4-gram overlap ระหว่างข้อความของกำแพงกับข้อความของจุด)
           + 0.6 × (ลำดับความสนใจของจุด ปรับเป็นสัดส่วนกับจุดที่สูงสุดในคลัง)
           + 0.5 × (คะแนนที่จุดนี้เคยได้จากการช่วย/ไม่ช่วยทำลายกำแพง)
แล้วคัดโดยจำกัด "โดเมนละไม่เกิน N จุด" ก่อน จึงเติมที่เหลือด้วยความเกี่ยวล้วน
```

ตัววัดคือ Jaccard บน 4-gram ตัวเดียวกับที่ Layer 6.5 ใช้ตัดสินว่าสองรอบที่ล้มเหลวเป็นวิธีเดียวกันไหม
เพียงแต่ชี้ไปที่กำแพงแทนที่จะชี้ไปที่รอบ (ข้อความเป็นภาษาไทยที่ไม่มีช่องว่างให้ตัดคำ 4-gram จึงเป็นตัววัดเดียวที่ใช้ได้ทั้งสองภาษา)

**ทำไมต้องถ่วง ไม่ใช่ใช้ overlap ล้วน** — overlap ล้วนจะคายออกมาแต่จุดที่พูดถึงกำแพงด้วยถ้อยคำของกำแพงเอง
ซึ่งเป็นสิ่งที่ *ตรงข้าม* กับการเชื่อมข้ามโดเมน · การถ่วงด้วยลำดับความสนใจและเพดานโดเมนคือสิ่งที่ทำให้
"การถ่วงน้ำหนักท้องเรือให้ตั้งลำเองเมื่อโดนคลื่น" มีสิทธิ์เข้าไปอยู่ในพรอมป์ตของกำแพงเรื่องพรอมป์ต

### สิ่งที่ทำให้มันเป็นลูป ไม่ใช่ของประดับ

รอบที่ได้อ่านความรู้เพิ่มขึ้นแล้วไม่มีใครรู้ว่ามันใช้อะไรบ้าง ก็แค่พรอมป์ตที่ยาวขึ้น ดังนั้น:

```
รอบหลอมตัวเองต้องตอบ  "used_dot_ids": ["dot_..."]  และ  "used_principle": "หลักการนั้นคืออะไร และทาบอย่างไร"
        ↓
ระบบเขียนคำประกาศนั้นกลับเป็น "การเชื่อมจุด" จริงใน data/connections.json (มี field `forge`)
        ↓
outcome ของการเชื่อมนั้น ถูกให้คะแนนโดย "ด่านทั้งหมดของรอบนั้น" ไม่ใช่โดยมนุษย์:
        รอบผ่านทุกด่าน  → shipped 5/5 → สัญญาณ +1   (จุดนั้นได้เครดิตเท่าไอเดียที่ถูกเอาไปทำจริง)
        รอบตก/ย้อนกลับ  → dead 1/5    → สัญญาณ −1   (จุดนั้นถูกหักเท่าไอเดียที่ตายแล้ว)
        ↓
enrichDots() ตัวเดิมพับมันลงบน value_score → attention_score → พรอมป์ตรอบถัดไป (Layer 4.5 ไม่ถูกแก้แม้แต่บรรทัดเดียว)
        ↓
และแยกออกมาอีกช่องหนึ่ง: forge_uses / forge_score / forge_proven — "จุดนี้ช่วยทำลายกำแพงในโค้ดได้จริงไหม"
```

**นี่คือกลไกแรกที่บอกระบบได้ว่าความรู้ชิ้นหนึ่งของมันไร้ค่า** คลังที่ได้แต่เครดิตอย่างเดียวเป็นคลังที่ผิดไม่ได้
และคลังที่ผิดไม่ได้ก็โตได้ไม่จำกัดโดยไม่มีใครรู้ว่าอะไรในนั้นใช้ได้จริง

กติกาที่ทำให้มันซื่อสัตย์:

- **ไม่ประกาศ = ไม่มีอะไรถูกเขียน** ระบบไม่เดาแทนว่ารอบนั้นน่าจะใช้จุดไหน (การเดาแทนคือการปลอมหลักฐาน)
  รอบที่ตอบ `used_dot_ids: []` ถูกบันทึกว่า "ได้อ่านคลังแล้วแต่ไม่ได้ประกาศ" — และพรอมป์ตสั่งให้อธิบายด้วยว่า
  ทำไมความรู้ที่มีจึงทาบกับกำแพงนี้ไม่ได้ ซึ่งเป็นข้อมูลที่มีค่ากว่าการอ้างจุดแบบขอไปที
- **id ที่อ้างแต่ไม่มีในคลัง ถูกรายงาน ไม่ถูกทิ้งเงียบ** (เหมือน `adoption.ignored` ของ Layer 6.8)
- **รอบที่เกิดก่อนชั้นนี้ ไม่ถูกให้คะแนนย้อนหลัง** — มันไม่เคยได้อ่านคลัง จึงถูกรายงานว่า `undeclared` ตรง ๆ
- **รอบยุบรวมไม่ถูกนับ** มันไม่ได้ทำลายกำแพงด้วยหลักการ จึงไม่ใช่หลักฐานของคลัง
- **รอบที่ล้มเหลวก็ถูกนับ** — และนี่คือหัวใจ ถ้านับแต่รอบที่ผ่าน คะแนนของทุกจุดจะเป็นบวกเสมอ

### ลำดับที่เปลี่ยนไปใน `attemptEvolution()` (และบั๊กที่โผล่ออกมาเพราะมัน)

```
เดิม   before = readSelf() → guarded = readGuarded() → เลือกกำแพง (introspect() อาจเขียน limits.json!) → ยิงโมเดล
ใหม่   before = readSelf() → เลือกกำแพง → รอบเชื่อมจุดเล็งกำแพง (เขียน connections.json) → guarded = readGuarded() → ยิงโมเดล
```

การถ่ายภาพความทรงจำต้องเกิด **หลัง** ขั้นตอนที่ได้รับอนุญาตให้เขียน `data/` โดยเจตนา
มิฉะนั้นทั้งรอบเชื่อมจุดของชั้นนี้ **และ `introspect()` ที่มีอยู่เดิม** จะดูเหมือนรอบหลอมตัวเองแอบแก้ความทรงจำของตัวเอง
แล้วตกด่าน guard ทันทีในเทิร์นแรก (เส้นทางเดิมนั้นเป็นบั๊กที่ยังไม่มีใครเจอ เพราะมันโผล่เฉพาะตอนที่ทะเบียนขอบเขตว่างเปล่า)

ส่วนการ **เขียนคำประกาศกลับเข้าคลัง** ต้องเกิด *หลังคำตัดสิน* ไม่ใช่ระหว่างด่าน — ระหว่างด่าน
การเขียนแบบเดียวกันนี้จะถูกจับได้ว่าเป็นการแตะความทรงจำ **ซึ่งถูกต้อง** และไม่มีการผ่อนปรนกฎข้อนั้นเลยแม้แต่นิดเดียว

### ตรวจเองได้โดยไม่เสียรอบ AI

`POST /api/forge/knowledge/dryrun` เรียก `forgeKnowledge()` / `wallRelevance()` / `forgeFeedbackPlan()` /
`forgeFeedbackRecord()` **ตัวจริง** ด้วยกำแพงสมมติ คลังสมมติ และรอบสมมติ (ไม่ใช้ AI ไม่เขียนอะไรลงคลัง)
จึงตรวจได้ทันทีว่า: จุดที่ทาบกับกำแพงได้ถูกจัดอันดับสูงกว่าจริงไหม · เพดานโดเมนดันจุดจากโดเมนอื่นเข้ามาจริงไหม ·
และกรณีที่สำคัญที่สุด: **การประกาศว่าใช้จุดหนึ่งในรอบที่ *ตก* ทำให้คะแนนของจุดนั้นติดลบจริงหรือไม่**

### ข้อจำกัดที่ต้องพูดตรง ๆ

**ผลข้างเคียงที่ตั้งใจให้เกิด** — บันทึกของรอบหลอมตัวเองอยู่ใน `connections.json` เดียวกับไอเดีย จึงพาสองอย่างตามมา:
(1) จุดที่รอบซึ่ง *ผ่าน* อ้างว่าใช้ จะกลายเป็นจุด `proven` แล้ว **Layer 08 จะยุบรวมมันไม่ได้อีก** (จุดที่พิสูจน์แล้วถูกล็อกไว้เด็ดขาด)
— นี่คือสิ่งที่ควรเกิด: จุดที่เคยช่วยทำลายกำแพงได้จริงคือจุดที่ต้องรอดมาในฐานะตัวมันเอง
(2) `buildLessons()` **กรองบันทึกเหล่านี้ออกโดยปริยาย** เพราะมันเป็นบทเรียนเรื่องตัวเครื่องยนต์ ไม่ใช่เรื่องไอเดีย —
ถ้าไม่กรอง รอบหลอมตัวเองติด ๆ กันจะเบียดไอเดียจริงออกจากบล็อก "รอบก่อนอะไรได้ผล" ของ Layer 4.5 ทั้งหมด

คำประกาศ `used_dot_ids` เป็น **คำพูดของโมเดล** ไม่ใช่การวัด: ระบบตรวจได้ว่า id นั้นมีจริงและอยู่ในบล็อกที่ส่งให้จริง
แต่ตรวจไม่ได้ว่า "ใช้จริง" หรือ "หยิบมาอ้างให้ดูดี" — สิ่งที่กันไว้คือ *แรงจูงใจ*: การอ้างจุดในรอบที่ตกทำให้จุดนั้นเสียคะแนน
ดังนั้นการอ้างพรวดพราดจึงไม่ฟรี · การพิสูจน์ว่าหลักการหนึ่งถูกใช้จริงในระดับโค้ดต้องเทียบ diff กับหลักการนั้น
ซึ่งเป็นกำแพงของรอบถัดไป ไม่ใช่รอบนี้

## 🧬 Layer 11–15 — ห้าชั้นที่ปิดวงจรการหลอมตัวเอง

ห้าชั้นนี้ถูกทำลายในรอบเดียวกัน เพราะเมื่อเรียงต่อกันแล้วมันคือวงจรเดียว:

```
      ┌─────────────────────────────────────────────────────────────────┐
      │  Layer 11  กระจกเห็นสิ่งที่มันทำจริง ไม่ใช่แค่สิ่งที่โค้ดบอกว่ามันควรทำ     │
      │      ↓     (ตั้งชื่อกำแพงจากบันทึกจริง ไม่ใช่จากการอ่านซอร์สอย่างเดียว)   │
      │  Layer 12  อ่านตัวเองในงบที่คงที่ ไม่โตตามความสำเร็จของตัวเอง            │
      │      ↓                                                          │
      │  Layer 15  ลงแข่งหลายสายเลือดพร้อมกัน ไม่ใช่เดิมพันกับแนวทางเดียว        │
      │      ↓                                                          │
      │  Layer 13  ถูกตัดสินโดยผู้ตัดสินที่มันแก้ไม่ได้ และข้อสอบที่มันไม่ได้เขียน   │
      │      ↓                                                          │
      │  Layer 14  กลายเป็นโค้ดใหม่นั้นจริง ๆ ทันที โดยมีคนนอกคอยกู้กลับถ้าพลาด    │
      └──────────────────────────────┬──────────────────────────────────┘
                                     └──→ กลับไปที่ Layer 11 ด้วยพฤติกรรมใหม่ให้ส่องกระจก
```

ก่อนหน้านี้วงจรขาดทั้งห้าจุด: มันส่องกระจกเห็นแต่โค้ด · จ่ายค่าอ่านตัวเองแพงขึ้นทุกรอบ ·
เดิมพันกับแนวทางเดียว · ตรวจข้อสอบที่ตัวเองเขียนด้วยไม้บรรทัดที่ตัวเองถือ · แล้วนอนรอมนุษย์กดปุ่ม

### สิ่งที่วัดได้หลังทั้งห้าชั้น

| | ก่อน | หลัง |
|---|---|---|
| พรอมป์ตที่รอบหนึ่งต้องอ่าน | 570 KB และโตทุกรอบ | **15 KB มีเพดานตายตัว** (ประหยัด 97%) |
| สิ่งที่กระจกเห็น | ซอร์สโค้ดอย่างเดียว | ซอร์ส + บันทึก 7 วัน + ด่านที่ตก + เส้นทางที่ไม่มีใครเรียก |
| ผู้ตัดสิน | ฟังก์ชันในไฟล์ที่ผู้ถูกตัดสินแก้ได้ | สัญญาแช่แข็ง + ผู้ตรวจอิสระที่รันทุกรอบ |
| ผู้ออกข้อสอบ | เซสชันเดียวกับผู้เขียนโค้ด (และแก้ข้อสอบได้หลังเห็นคะแนน) | เซสชันแยกที่ไม่เห็น diff |
| ผู้สมัครต่อรอบ | 1 | **2** (ปรับได้ 1–5 ผ่าน `FORGE_POPULATION`) |
| หลังรอบผ่าน | รอมนุษย์กดรีสตาร์ต | รีสตาร์ตเองใน ~1.3 วินาที + สุนัขเฝ้าบ้านกู้กลับถ้าพลาด |

### สิ่งที่ยังต้องระวัง (พูดตรง ๆ)

- **Layer 15 ยังไม่เคยวิ่งกับรอบ Opus จริง** — ตรรกะการเลือกและเครื่องจักรการแข่ง (ทรีจริง บูตจริง
  กวาด endpoint จริง) ถูกพิสูจน์แล้วโดยแทนเฉพาะเซสชัน AI ด้วยตัวปลอม · ถ้าการแข่งพังด้วยเหตุใดก็ตาม
  รอบจะ**ถอยกลับไปเป็นรอบเดี่ยวแบบเดิมอัตโนมัติ** ไม่ใช่พังทั้งรอบ · ตั้ง `FORGE_POPULATION=1` เพื่อปิด
- **Layer 13 ส่วนผู้ออกข้อสอบตาบอด** เปิดใช้ได้ใน `verifier/contract.json` (`blind_proof.enabled`)
  และกลไกถูกพิสูจน์แล้วว่าไม่ส่ง diff ให้ผู้ออกข้อสอบ — แต่การเปิดจริงเพิ่มเซสชัน Opus ต่อรอบ
- **Layer 14 รีสตาร์ตอัตโนมัติผูกกับสวิตช์ Layer 9** — ปิดอยู่ตราบใดที่ 🔥 Self-Forge ยังไม่ถูกติดอาวุธ

## 🧲 Layer 10 — The RemLedger: ทะเบียนที่ลืมไม่ได้

**ชั้นนี้ไม่ได้ถูกคิดโดยมนุษย์** ระบบถูกสั่งให้เอาเครื่องมือเชื่อมจุดของตัวเองมาเล็งกำแพงของตัวเอง
(`POST /api/forge/insight` · Layer 06.9) แล้วมันหยิบสองจุดจากคลังที่ไม่เกี่ยวอะไรกับซอฟต์แวร์เลย:

| จุด | โดเมน | โครงสร้างที่มันให้ |
|---|---|---|
| ฮิสเทอรีซิส: ทางไปกับทางกลับไม่ใช่เส้นเดียวกัน | ฟิสิกส์ | แม่เหล็กเหลือ remanence เมื่อสนามภายนอกเป็นศูนย์ — **ต้องออกแรงย้อนศรถึงระดับ coercive ถึงจะล้างได้** |
| ภูมิคุ้มกันแบบจดจำ: แข็งแรงขึ้นเพราะเคยพัง | ชีววิทยา | memory cell จำ**แอนติเจน** ไม่ได้จำหมายเลขเคส — ตัวตนถูกระบุด้วยรูปร่าง ไม่ใช่ฉลากที่ออกใหม่ |

> "การยืนยันว่ากำแพงมีอยู่ควรราคาถูก แต่การประกาศว่ากำแพงหายไปต้องจ่ายแรงย้อนศรถึงระดับวิกฤต
> ระหว่างนั้นสถานะค้างอยู่เอง โดยไม่ต้องมีใครเอ่ยถึงในรอบนั้น" — ข้อความจากรอบเชื่อมจุด `conn_6b842de6`

### กำแพงที่มันเจอในตัวเอง

```js
// โค้ดเดิมของ introspect() — ทั้งไฟล์ถูกเขียนทับทุกครั้งที่ส่องกระจก
const broken = current.filter((l) => l.status === "broken");
const fresh  = incoming.map(...);              // เฉพาะข้อที่โมเดลพูดถึงรอบนี้
saveJson(LIMITS_FILE, [...broken, ...fresh]);  // ← ที่เหลือหายไปเงียบ ๆ
```

ผลสามชั้นที่ไม่มีใครมอง:

1. **กำแพงที่ยังจริงอยู่แต่ไม่ถูกเอ่ยถึง หายทั้งดวง** พร้อม `attempts` ที่สะสมมา
2. **แผลเป็นขาดจากเจ้าของ** — `failedRounds()` ค้นด้วย `limit.id` ล้วน ๆ กำแพงเดิมที่ได้ id ใหม่จึงกลายเป็น
   กำแพงที่ไม่เคยล้มเหลวมาก่อน · `attemptBudget()` เริ่มนับหนึ่งใหม่ · ระบบลองวิธีเดิมซ้ำได้ไม่จำกัดโดยเชื่อว่าเป็นครั้งแรกทุกครั้ง
3. **ความจำของความล้มเหลว (Layer 06.5) ตั้งอยู่บนฐานที่ลบตัวเองได้** — ชั้นที่ 6.5 ถูกสร้างมาเพื่อไม่ให้บทเรียนหาย
   แต่ไม่มีใครถามว่าฐานของมันเป็น append หรือ overwrite เพราะสองอย่างนี้ดูเหมือนกันในวันที่โมเดลบังเอิญคืน id เดิม

### สถานะของกำแพงหลังชั้นนี้

```
                    ถูกเอ่ยถึง                      เงียบ 1-2 รอบ            เงียบครบ 3 รอบ
   ┌──────────┐  ────────────→  ┌──────────┐  ────────────→  ┌──────────┐  ──────→  ┌─────────┐
   │  พบใหม่   │                │ standing │                │ remanent │          │ dormant │
   └──────────┘  ←────────────  └──────────┘  ←──────────────────────────────────  └─────────┘
                   ทำลายสำเร็จ         │              เอ่ยถึงอีกครั้ง = ตื่นพร้อมประวัติครบ
                                       ↓
                                 ┌──────────┐   ย้อนกลับรอบนั้น
                                 │  broken  │  ─────────────→ standing
                                 └──────────┘
```

**ไม่มีลูกศรไหนชี้ออกนอกทะเบียน** — ไม่มีสถานะไหนคือการถูกลบ `dormant` แปลว่า "เลิกแย่งรอบวิวัฒนาการ"
ไม่ได้แปลว่า "ไม่มีอยู่" และการเอ่ยถึงเพียงครั้งเดียวก็พากลับมาพร้อม `attempts` เดิม แผลเป็นเดิม และ alias ทั้งหมด

### Epitope: จับคู่จากรูปร่าง ไม่ใช่จากฉลาก

```
epitopeOf(wall) = {
  shingles: 4-gram ของ title+description+evidence หลัง normalize   // รอดจากการเขียนใหม่
  symbols:  ตัวระบุในโค้ดที่กำแพงอ้างถึง (ฟังก์ชัน · ไฟล์ · /api/*)   // รอดจากการเปลี่ยนสำนวนทั้งย่อหน้า
}
similarity = max(prose, prose·0.5 + code·0.5)     ต้อง ≥ 0.42 ถึงจะนับว่าเป็นกำแพงเดียวกัน
```

สัญลักษณ์ในโค้ดมีน้ำหนักกว่าเนื้อความโดยตั้งใจ: กำแพงสองข้ออาจบรรยายโค้ดชิ้นเดียวกันด้วยคนละสำนวน
แต่กำแพงสองข้อที่ชี้ไปที่ฟังก์ชันเดียวกันสามตัวและรูปแบบความล้มเหลวเดียวกัน ไม่ใช่กำแพงสองข้อ
เกณฑ์ไม่ถูกตั้งให้กระหายเกินไปด้วยเหตุผลตรงข้าม: **การรวมกำแพงจริงสองข้อเข้าด้วยกัน คือการซ่อนไปหนึ่งข้อ**

### พิสูจน์ได้โดยไม่ต้องจ่ายรอบส่องกระจก

`mergeLimits()` ถูกแยกออกมาเป็นฟังก์ชันบริสุทธิ์ (ไม่แตะดิสก์ ไม่เรียก AI) และเปิดออกทาง
`POST /api/limits/merge/dryrun` — พฤติกรรมที่ตัดสินว่าประวัติของกำแพงจะรอดหรือไม่
จึงถูกตรวจได้ในหนึ่งวินาทีด้วยทะเบียนสมมติ แทนที่จะต้องจ่ายรอบส่องกระจก 12 นาทีเพื่อดูผลครั้งเดียว

ไฟล์พิสูจน์ `selftest/remledger.js` (26 ข้อ) ตรวจครบทุกเส้นในแผนภาพข้างบน รวมทั้งข้อที่สำคัญที่สุดสองข้อ:
กำแพงที่เขียนใหม่ทั้งประโยคและได้ id ใหม่ต้อง**ถูกจับคู่กลับ** (ความคล้าย 0.88) และกำแพงคนละเรื่องต้อง
**ไม่ถูกกลืนรวมกัน**

## 🔑 Layer 09 — The Ignition: สวิตช์กุญแจ

ชั้น 00–08 ทุกชั้นตอบคำถามว่า *เครื่องยนต์ควรฉลาดขึ้นอย่างไร* ไม่มีชั้นไหนตอบว่า **ใครเป็นคนตัดสินใจว่ามันจะเริ่มเมื่อไหร่**

```
ก่อน Layer 09                        หลัง Layer 09
────────────────────────────────────────────────────────────────────────
node server.js                       node server.js
  → 90 วินาทีต่อมา daemon ตื่น         → เครื่องยนต์บูตแบบ "หยุด" · ไม่มีอะไรตื่น
  → เก็บเกี่ยว inbox เอง               → ทุกปุ่มในหน้าเว็บใช้ได้ครบเหมือนเดิม
  → ครบ 12 ชม. ออกไปค้นเว็บเอง         → กด ▶ เปิดเดินเครื่อง เมื่อพร้อม
  → ครบ 24 ชม. เขียนซอร์สโค้ดตัวเองใหม่  → Self-Forge ยังปิดอยู่ ต้องติ๊กเปิดแยกอีกชั้น
วิธีหยุด: แก้ env แล้วรีสตาร์ต          วิธีหยุด: กดปุ่ม (และ 🛑 ฆ่างานที่ค้างอยู่ได้)
```

**หลักการเดียวที่ทั้งชั้นนี้ตั้งอยู่บนนั้น: สวิตช์คุม *นาฬิกา* ไม่ได้คุม *ความสามารถ*.**
เครื่องยนต์ที่หยุดอยู่ยังเชื่อมจุดได้ ยังพิสูจน์ไอเดียได้ ยังทำลายกำแพงตัวเองได้ — เมื่อคุณสั่ง
สิ่งที่หายไปคือความสามารถของมันที่จะ**เริ่มเอง**

### สวิตช์

| สวิตช์ | ค่าตั้งต้น | ปิดอยู่แล้วเกิดอะไร |
|---|---|---|
| **เดินเครื่องอัตโนมัติ** (สวิตช์ใหญ่) | ปิด | ไม่มีรอบตามเวลาใด ๆ เลย — daemon ตื่นแล้วกลับไปนอนทันทีโดยไม่อ่าน ไม่เขียน ไม่จ่ายอะไร |
| ⚡ เชื่อมจุดเอง | เปิด (มีผลเมื่อสวิตช์ใหญ่เปิด) | inbox ยังเก็บเกี่ยวได้ด้วยปุ่ม แต่จะไม่มีรอบ Opus ไหนเกิดขึ้นเองอีก |
| 🌐 The Scout | เปิด (มีผลเมื่อสวิตช์ใหญ่เปิด) | ไม่ออกเว็บเองตามเวลา · ปุ่ม "ออกไปหาความรู้ใหม่เอง" ยังทำงาน |
| ⚗ The Distiller | เปิด (มีผลเมื่อสวิตช์ใหญ่เปิด) | ไม่ยุบรวมคลังเองตามเวลา · ปุ่มสั่งรอบยุบรวมยังทำงาน |
| 🔥 **Self-Forge** | **ปิดเสมอ** | ไม่เขียนซอร์สโค้ดตัวเองตามเวลา · ปุ่ม 🔥 ทำลายขอบเขต ยังทำงาน |

สวิตช์ 🔥 คือข้อเดียวที่ **เปิดสวิตช์ใหญ่แล้วไม่ถูกลากตามมา** และหน้าเว็บถามยืนยันทุกครั้งที่ติ๊ก
เหตุผลตรงไปตรงมา: ทุกสวิตช์อื่นเพิ่มหรือลดแถวในไฟล์ข้อมูล สวิตช์นี้เปลี่ยนโปรแกรมที่คุณกำลังใช้อยู่

### 🛑 หยุดฉุกเฉิน

รอบหลอมตัวเองใช้เวลาได้ถึง 45 นาที และเดิม**ไม่มีทางหยุดกลางคัน** — ทางเดียวคือฆ่าเซิร์ฟเวอร์
ซึ่งทิ้งซอร์สทรีที่เขียนค้างไว้ครึ่งทาง ตอนนี้เครื่องยนต์ถือ handle ของทุกโปรเซส `claude` ที่มันเปิด:
ปุ่ม 🛑 ปิดสวิตช์ใหญ่ **และ** ฆ่างานที่กำลังทำอยู่ในคำสั่งเดียว รอบที่ถูกฆ่าจะตกด่านของตัวเอง
แล้วเดินเข้าเส้นทาง rollback เดิมที่ระบบรู้จักอยู่แล้ว — ไม่มีสถานะ "ถูกยกเลิก" พิเศษให้ต้องดูแล

(รายละเอียดที่ทำให้มันหยุดได้จริง: ลูป retry ของ `runClaudeRaw()` จะไม่ปลุกงานที่เพิ่งถูกสั่งหยุดขึ้นมาใหม่
เพราะทุกคำขอจำ "ยุคของการหยุด" ที่มันเกิด และบน Windows ใช้ `taskkill /T` เพราะการฆ่า `cmd.exe`
เฉย ๆ จะทิ้ง CLI ลูกไว้เผาโทเคนต่อโดยไม่มีใครฟัง)

### สวิตช์นี้ Self-Forge แตะไม่ได้

`evolution/autopilot.json` อยู่นอก `walkSelf()` และถูกอ่านกลับไบต์ต่อไบต์โดย `readGuarded()` เหมือน
`data/` กับ `verifier/` — รอบที่ติดอาวุธให้ตัวเองจึงเป็นรอบที่เป็นโมฆะ และ `/api/autopilot` ถูกใส่ไว้ใน
`REGRESSION_ENDPOINTS` ด้วย: รอบที่ทำให้ปุ่มหยุดพังคือรอบที่ตก ต่อให้ทุกอย่างที่เหลือดีเลิศ

ไฟล์พิสูจน์: `selftest/ignition.js` (24 ข้อ) — ตรวจว่าทรีที่เพิ่งเกิดบูตมาแบบหยุดเสมอ · สวิตช์ใหญ่ครอบย่อยจริง ·
เปิดใหญ่ไม่ปลดล็อก Self-Forge · สถานะรอดข้ามการรีสตาร์ต · ค่าที่ไม่ใช่ boolean ถูกปฏิเสธด้วย 400

### ตรวจก่อนเริ่ม (preflight)

เครื่องยนต์ทั้งเครื่องพิงคำสั่งเดียวคือ `claude` และเดิมสิ่งแรกที่บอกคุณว่ามันหายไปคือรอบสี่นาทีที่ตายพร้อมข้อความ
`claude exited 1` ตอนนี้เซิร์ฟเวอร์ถามครั้งเดียวตอนบูต แล้วขึ้นแถบแดงบนหน้าเว็บถ้าเรียกไม่ได้

## ⚗ Layer 08 — The Distiller: คลังที่ยุบรวมตัวเองได้โดยพิสูจน์ได้

Layer 06.6 หาว่าฟังก์ชันความเหมาะสมของ *โค้ด* อนุญาตให้โตอย่างเดียว แล้วแก้ด้วยรอบชนิดที่สอง
แต่กำแพงรูปทรงเดียวกันฝั่ง *ความรู้* ไม่มีใครแตะ — และ Layer 07 เพิ่งเร่งอัตราการโตของมันขึ้นอีก

```
ทางเข้าของจุด (ทั้งหมดเป็นการเพิ่มล้วน ๆ)     ทางออกของจุด
  · /api/capture                              · มนุษย์กด DELETE /api/dots/:id
  · inbox/*.txt|md                               (จบ)
  · harvestEvidenceDots({max: 50}) ทุกรอบ daemon
  · scoutWeb() ทุก SCOUT_HOURS ชั่วโมง
  · หนึ่งจุดต่อหนึ่งรอบวิวัฒนาการที่ผ่าน
```

และปลายทางของทุกทางเข้านั้นคือบรรทัดเดียว: `buildConnectPrompt()` แปลง **ทุกจุด** เป็นระเบียนเต็ม
รวม `d.content` ครบทุกตัวอักษร ไม่มีเพดาน ไม่มีการตัด — จุดที่ถูกทำเครื่องหมายว่า `forgotten`
ก็ยังถูกอ่านเต็ม ๆ ทุกรอบ **"การลืม" ของระบบจึงเป็นแค่ป้ายสี ไม่ใช่กลไก**
พรอมป์ตที่ระบบต้องอ่านก่อนคิดได้จึงยาวขึ้นเชิงเส้นตามทุกอย่างที่มันเคยถูกบอกมา ตลอดกาล

Layer 08 ยืมยาขนานเดิมของ 06.6 มาใช้ โดยชี้ไปที่จุดแทนไฟล์:

| | รอบขยาย (06) | รอบยุบรวมโค้ด (06.6) | **รอบยุบรวมความรู้ (08)** |
|---|---|---|---|
| ไฟล์พิสูจน์กับของใหม่ | ต้องผ่าน | ต้องผ่าน | — |
| ไฟล์พิสูจน์กับของเดิม | ต้อง **ตก** | ต้อง **ผ่าน** | — |
| เกณฑ์ "ไม่มีอะไรหาย" | regression sweep | ชุดทดสอบสะสมให้ผลเหมือนกันทุกไฟล์ | **ทุกการเชื่อมที่เคยได้สัญญาณบวกยังอธิบายได้ + จุดที่ proven ไม่หาย** |
| เกณฑ์ความคืบหน้า | มีความสามารถใหม่ | ตัวเลขขนาดโค้ดลดลง | **จำนวนตัวอักษรของ `buildConnectPrompt` ลดลงจริง** |
| ย้อนกลับ | `evolution/backups/` | `evolution/backups/` | **`data/knowledge.json` เก็บจุดเดิมทั้งดวง** |
| สั่งได้ที่ | `POST /api/transcend` | `POST /api/consolidate` | `POST /api/knowledge/distill` |

### สองกลไก ไม่ใช่กลไกเดียว

**(1) เพดานพรอมป์ต — การลืมจริงครั้งแรกของระบบ**

```
จุดเรียงตาม attention_score อยู่แล้ว (ความหายาก × คุณค่าที่พิสูจน์แล้ว)
  หัว CONNECT_FULL_DOTS จุดแรก  → แสดงเต็ม (content ถูกตัดที่ CONNECT_DOT_CHARS ตัวอักษร)
  ที่เหลือทั้งหมด               → ย่อเหลือ "- id · โดเมน · ชื่อจุด" บรรทัดเดียว
  จุดที่ mustInclude (Serendipity Revival) → ถูกดึงขึ้นหัวเสมอ ไม่ว่าอยู่อันดับไหน
```

ไม่มีจุดไหน *หายไปจากการเลือก* — ทุกจุดยังมี id ให้หยิบ สิ่งที่ถูกลืมคือ "เนื้อหา" ไม่ใช่ "การมีอยู่"
นี่คือเหตุผลที่พรอมป์ตหยุดโตเชิงเส้น: จุดที่ 41 ถึงจุดที่ 4,000 คิดค่าใช้จ่ายเท่ากันบรรทัดละครั้ง
`GET /api/knowledge/preview` รายงาน `unbudgeted_chars` (ถ้าไม่มีเพดานจะกินเท่าไหร่) เทียบกับ `budgeted_chars` จริง

**(2) รอบยุบรวม — จุดซ้ำซ้อนกลายเป็นจุดหลักการเดียว**

```
หาความซ้ำซ้อนเอง   วัด Jaccard บน 4-gram ของ (ชื่อจุด + เนื้อหา) — ตัววัดเดียวกับที่ Layer 06.5
                    ใช้ตัดสินว่าสองรอบที่ล้มเหลวเป็นวิธีเดียวกันไหม เพียงแต่ชี้ไปที่จุดแทนรอบ
                    เกณฑ์ DISTILL_SIMILARITY · ไม่ใช้ AI ไม่ออกเน็ต · ดูได้ก่อนที่ปุ่ม 🔎
AI เสนอแผน          The Distiller เขียน "จุดหลักการ" หนึ่งจุดมาแทนทั้งกลุ่ม (ต้องเป็นนามธรรมขึ้นหนึ่งขั้น
                    ไม่ใช่การต่อข้อความของเดิมเข้าด้วยกัน) — ระบบไม่เชื่อคำพูดของมันแม้แต่คำเดียว
ด่านตัดสิน          knowledgeVerdict() ตรวจสามข้อด้วยตัวเลขจริง ตกข้อใดข้อหนึ่ง = คลังไม่ถูกแตะเลย
เก็บของเดิม         จุดเดิมทุกจุดถูกเก็บทั้งดวงใน data/knowledge.json + จุดหลักการพก id เดิมติดตัวใน
                    distilled.absorbed[] — จึงชี้กลับได้เสมอว่าความรู้เดิมไปอยู่ที่ไหน
ย้อนกลับ            POST /api/knowledge/restore คืนจุดเดิมและถอนจุดหลักการออก ครบทุกตัวอักษร
```

### ทำไม "การดูดกลืน" ถึงเป็นหัวใจ ไม่ใช่การลบ

รอบนี้ **ลบจุดไม่ได้เลย** — ทำได้อย่างเดียวคือ *ดูดกลืน*: จุดที่หายจากคลังต้องมี id และชื่อของมัน
อยู่ใน `distilled.absorbed[]` ของจุดหลักการที่มาแทน นั่นคือสิ่งที่ทำให้ด่านที่ 1 พิสูจน์ได้จริง —
ทุกการเชื่อมที่เคยได้ผลจริงยังชี้กลับไปหาจุดที่มันสร้างมาได้ครบ แม้จุดนั้นจะไม่กินที่ในพรอมป์ตอีกแล้ว
และเมื่อยุบรวมซ้อนกันหลายรอบ รายการ `absorbed` จะถูก **แบนราบ** (จุดที่เคยเป็นหลักการส่งลูกของมันต่อ)
จึงไม่มีจุดไหนหลุดออกนอกระยะเอื้อมเพราะถูกยุบรวมสองครั้ง

จุดที่ `proven` (คะแนนคุณค่าจากผลลัพธ์จริง ≥ 0.34) **ถูกล็อกไว้เด็ดขาด**: การดูดกลืนมันนับเป็นการทำหาย
มันต้องรอดมาในฐานะตัวมันเอง เพราะจุดที่ *พิสูจน์แล้ว* ว่าให้ผลจริงคือสิ่งเดียวที่คลังนี้ยืนยันได้ว่ามีค่า

`POST /api/knowledge/dryrun` เรียกฟังก์ชันความเหมาะสมตัวจริงด้วยแผนสมมติ (หรือคลังสมมติทั้งใบ) ได้โดยไม่เสียรอบ AI
ใช้ตรวจเองได้ว่ากรณีไหนผ่านกรณีไหนตก — รวมถึงกรณีที่สำคัญที่สุด: **แผนที่กลืนจุด proven ต้องถูกปฏิเสธเสมอ**

## Layer 07 — The Scout: ความรู้ที่เดินเข้ามาเอง

```
🧪 กู้หลักฐานที่เคยค้นเจอ   ไม่ใช้ AI ไม่ออกเน็ต ไม่เสียเงิน — แค่เลิกทิ้งของที่จ่ายไปแล้ว
                            connections.json → evidence.novelty.similar_found ทุกชิ้น
                            → ตัดวงเล็บอ้างอิงออกเป็นชื่อจุด · เดาโดเมนจากคำสำคัญ (fallback "โลกภายนอก")
                            → เก็บ how_close เป็นเนื้อหา + แนบ URL ที่ค้นเจอจริง
                            → กันซ้ำด้วย key ที่ normalize แล้ว (รันกี่ครั้งก็ไม่เกิดจุดซ้ำ)
🕳 ช่องว่างของโดเมน         สำมะโนโดเมนในคลัง เทียบกับโดเมนอ้างอิงกว้าง ๆ 20 สาขา
                            → โดเมนที่ "ไม่มีจุดใดเลย" ถูกตั้งชื่อออกมาตรง ๆ
                            → ระบบเขียนคำค้นของตัวเองจาก (ช่องว่าง × จุดที่มันสนใจที่สุดตอนนี้)
                            → คำค้นเป็นค่าคงที่คำนวณได้ (deterministic) ผู้ใช้อ่านก่อนได้ว่ามันจะค้นอะไร
🌐 ออกไปหาเอง               daemon ทุก SCOUT_HOURS ชม. (หรือกดเอง) → WebSearch จริงในโดเมนที่ขาด
                            → ได้จุดใหม่ origin: scout:web พร้อมแหล่งอ้างอิง → toast แจ้งเตือน
🚫 สิทธิ์ยับยั้งของเจ้าของ   ทุกจุดติดป้ายที่มา 4 แบบ: 👤 เจ้าของ · 🧪 หลักฐาน · 🌐 เว็บ · 🔥 การหลอมตัวเอง
                            จุดที่ระบบหามาเองมีปุ่ม 🚫 — กดแล้วจุดหายและ "คีย์" ของมันถูกจำไว้ถาวร
                            ระบบจึงหยิบสิ่งเดิมกลับเข้าคลังไม่ได้อีก แม้จะค้นเจอซ้ำ
```

**ทำไมถึงปลอดภัยกับคลังของเจ้าของ**

- จุดที่ระบบหามาเอง **ไม่เคยปลอมตัวเป็นของคุณ**: มี `origin` ติดตัวเสมอ, การ์ดบนหน้าเว็บเป็นเส้นประ, และพรอมป์ตรอบเชื่อมจุดก็เห็นที่มาด้วย
- การกดลบจุดที่ระบบหามาเอง **นับเป็นการยับยั้ง** ไม่ใช่แค่ลบ — `DELETE /api/dots/:id` บันทึกคีย์ไว้ให้ The Scout จำ
- จุดที่ระบบหามาเอง **ไม่ปลุกรอบเชื่อมจุดที่เสียค่า AI ทันที** — ต้องรอ `AUTO_HOURS` เท่ากับการปลุกจุดที่ถูกลืม
- `SCOUT_HOURS=0` ปิดการออกเน็ตเองได้ทั้งหมด (การกู้หลักฐานยังทำงานอยู่ เพราะไม่ต้องออกไปไหน)
- ภายใต้ `DOT_SELFTEST=1` The Scout ไม่ออกอินเทอร์เน็ตเด็ดขาด เหมือนทุกชั้นที่เรียก AI

## Layer 06 — The Self-Forge ทำงานอย่างไร

```
🔬 วิเคราะห์ขอบเขตตัวเอง   อ่านทุกไฟล์ของตัวเอง + คลังจุด + นวัตกรรมที่เคยสร้าง
                            → ตอบว่า "อะไรคือกำแพงที่ขังฉันอยู่" 5-8 ข้อ พร้อมคะแนน unlock/risk
🔥 ทำลายขอบเขตนี้           เลือกกำแพงที่คุ้มที่สุด ที่ยังมีโควตาเหลือ แล้วลงมือ:
                            (ลำดับความคุ้ม = unlock − risk/2 ถ่วงด้วยคะแนนการถูกใช้จริงของหมวดนั้น — Layer 06.8)
   0. 🩹 ประกอบ "แฟ้มแผลเป็น" ของกำแพงนี้ → รอบที่เคยตกทั้งหมด: ตกที่ด่านไหน ข้อความจริงของด่าน
      ไฟล์ที่เคยแตะ + สรุป diff และพาธของโค้ดที่เคยลอง (evolution/backups/<evo-id>/attempted/)
      แล้วยัดเข้าหัวพรอมป์ตพร้อมคำสั่ง "ห้ามใช้วิธีเดิมซ้ำ"
   0.5 🧠 (Layer 06.9) เอาเครื่องมือเชื่อมจุดของตัวเองมาเล็งกำแพงนี้ก่อน: รันรอบเชื่อมจุดหนึ่งรอบโดยตั้ง
      focus = description + why_it_stands ของกำแพง (ใช้รอบที่ยังสดซ้ำได้ตาม FORGE_INSIGHT_HOURS · ล้มเหลวได้ไม่เป็นไร)
      แล้วประกอบ "บล็อกหลักการข้ามโดเมน" = จุดที่เกี่ยวกับกำแพงนี้ที่สุดพร้อมเนื้อหาจริง + นวัตกรรมที่ตัวเองเคยสังเคราะห์
      + บทเรียนจากเส้นตอบกลับ + hidden_pattern ของรอบที่เพิ่งเล็งกำแพง → ยัดเข้า *หัว* พรอมป์ตก่อนแผลเป็นและก่อนซอร์สโค้ด
      (บล็อกนี้มีเพดานตายตัว จึงไม่โตตามคลัง · ดูขนาดของทุกส่วนได้ที่ prompt_parts ใน /api/forge/preview)
   1. สำรองทุกไฟล์ก่อนแตะ         → evolution/backups/<evo-id>/
   2. Opus 5 (1M) แก้โค้ดจริงด้วย Read/Edit/Write/Bash(node --check) ในโฟลเดอร์นี้
      + บังคับให้เขียน "ไฟล์พิสูจน์" selftest/<evo-id>.js ที่รันได้จริง
      🔁 ตั้งแต่ Layer 06.7 ข้อ 3-9 ข้างล่างถูกรัน *ทันที* หลังโมเดลตอบ — ถ้าตกด่านไหน
         stdout จริงของด่านนั้นถูกป้อนกลับเข้าเซสชันเดิมเป็นเทิร์นถัดไป แล้ววนตรวจใหม่ทั้งชุด
         (สูงสุด FORGE_MAX_TURNS เทิร์น) · ย้อนกลับก็ต่อเมื่อเทิร์นหมดแล้วยังตกอยู่
   3. ตรวจว่าไม่แตะไฟล์ความทรงจำใน data/ (ถ้าแตะ = ยกเลิกทั้งรอบ)
   4. ไม่มีไฟล์พิสูจน์ = ตีตกทันที ไม่ว่าโค้ดจะสวยแค่ไหน
   4.5 📈 ด่าน declared_usage (Layer 06.8) — ต้องประกาศเส้นทางที่ความสามารถใหม่จะถูกใช้ผ่าน
       (จาก "usage_endpoints" ที่รอบนี้ตอบ + เส้นทาง /api/* ที่ diff เพิ่มเข้ามาจริง)
       ไม่เหลือเส้นทางเลย = ตีตก เพราะคำถาม "มีใครใช้ไหม" จะตอบไม่ได้ในภายหลัง
   5. node --check server.js
   6. บูตเซิร์ฟเวอร์ใหม่จริงบนพอร์ตสุ่ม แล้วยิง GET /api/dots ต้องได้ 200
   7. ▶ ด่านความสามารถ   สร้างสำเนาโค้ด "ใหม่" ในแซนด์บ็อกซ์ → รันไฟล์พิสูจน์ → ต้องผ่าน (exit 0)
   8. ▶ ด่านความต่าง     สร้างสำเนาโค้ด "เดิม" จาก backup → รันไฟล์พิสูจน์เดียวกัน → ต้องตก
                          (ถ้าผ่านทั้งคู่ = ความสามารถนี้มีอยู่ก่อนแล้ว = ไม่ได้ทำลายกำแพงจริง)
   9. ▶ ด่าน regression  ยิงทุก endpoint เดิม (/api/dots, /api/connections, /api/limits,
                          /api/self, /api/evolution, /api/forgotten, /api/serendipity/status,
                          /api/lessons, /api/forge/preview, /api/evolution/attempted)
                          ต้องได้ 200 ครบทุกเส้น
   9.5 🧠 (Layer 06.9) หลังคำตัดสิน: คำประกาศ "used_dot_ids" ของรอบนี้ถูกเขียนกลับเป็นการเชื่อมจุดจริงใน
       data/connections.json แล้วผลของด่านทั้งหมดกลายเป็น outcome ของมัน (ผ่าน → shipped 5/5 · ตก → dead 1/5)
       จุดที่ถูกอ้างจึงได้/เสียคะแนนตาม Layer 4.5 — คลังความรู้เริ่มถูกพิสูจน์ได้ว่าช่วยทำลายกำแพงจริงหรือไม่
       (เขียนหลังด่านจบเท่านั้น: ระหว่างด่าน การเขียน data/ แบบเดียวกันนี้ถูกจับว่าแตะความทรงจำ ซึ่งถูกต้องแล้ว)
  10. ผ่านครบ → บันทึกลง ledger + สร้างจุดความรู้ใหม่ "ทำลายขอบเขต: …" + toast
      📈 และบันทึก "คำสัญญา" ของรอบนี้ไว้ (adoption.endpoints + matures_at) — accepted ยังไม่ใช่คำตัดสินสุดท้าย
         อีก ADOPTION_DAYS วัน บันทึกการใช้งานจริงจะบอกว่ามีใครเรียกเส้นทางเหล่านั้นไหม (Layer 06.8)
      ขาดข้อใดข้อหนึ่ง → คืนไฟล์เดิมทั้งหมดอัตโนมัติ
      🩹 แต่ก่อนคืนไฟล์: สำเนาโค้ดที่เพิ่งเขียนถูกเก็บไว้ที่ evolution/backups/<evo-id>/attempted/
         และคำนวณใหม่ว่ารอบที่ตกนี้ "ต่างจากรอบก่อนจริงไหม" — ถ้าต่าง เพดานของกำแพงนี้ขยับขึ้น 1
🧹 รอบยุบรวม (Layer 06.6)  ทิศทางที่สอง: ไม่เพิ่มความสามารถ แต่รวบโค้ดซ้ำ ลบของที่ไม่มีใครใช้ ถอด endpoint ที่ตายแล้ว
                            ผ่านเมื่อ "ชุดทดสอบสะสมทั้งชุดให้ผลเหมือนกันทั้งกับโค้ดเดิมและโค้ดใหม่" + ตัวเลขขนาดลดลง + regression ครบ
                            📈 และตั้งแต่ Layer 06.8 มันถูก "เสนอเป้าหมาย" มาให้เอง: รายชื่อความสามารถที่บันทึกการใช้งาน
                            ตัดสินแล้วว่าไม่มีใครเรียกเลย ถูกส่งเข้าพรอมป์ตของรอบนี้ และรอบที่ 0 hit ติดกันครบเกณฑ์
                            ก็สั่งรอบยุบรวมได้เองโดยไม่ต้องรอครบ CONSOLIDATE_EVERY
♻️ รีสตาร์ตเครื่องยนต์      โปรเซสที่รันอยู่ปิดตัวเองแล้วเกิดใหม่ด้วยโค้ดที่ตัวเองเพิ่งเขียน
↩ ย้อนกลับ                 ทุกรอบที่สำเร็จย้อนกลับได้ตลอดเวลาจากหน้าเว็บ
```

### ฟังก์ชันความเหมาะสม: จาก "ไม่ตาย" เป็น "เก่งขึ้น"

เดิมรอบวิวัฒนาการได้ตราประทับ `accepted` เพียงเพราะ syntax ผ่านและบูตแล้วตอบ 200 —
แปลว่ารอบที่เขียนความสามารถใหม่แบบพังใช้ไม่ได้ หรือรอบที่แอบตัดความสามารถเดิมทิ้ง ก็ผ่านได้เหมือนกัน
ระบบจึง "เดินสุ่ม" ไม่ได้ "ไต่ขึ้น" ตอนนี้ทุกรอบต้องแนบหลักฐานที่รันได้เป็นโค้ด:

**สัญญาของไฟล์พิสูจน์ `selftest/<evo-id>.js`** (สคริปต์ Node เปล่า ไม่มี dependency):

| สิ่งที่ระบบส่งให้ | ความหมาย |
|---|---|
| `process.env.DOT_TEST_URL` | base URL ของเซิร์ฟเวอร์ที่บูตจากโค้ดที่กำลังถูกทดสอบ |
| `process.env.DOT_TEST_ROOT` | พาธของซอร์สทรีที่กำลังถูกทดสอบ (ห้ามใช้พาธตายตัว) |

`exit 0` = ความสามารถใหม่มีจริงและใช้ได้ · `exit ≠ 0` = ไม่มี · ต้องจบใน 60 วินาที · ต้องรันได้ออฟไลน์

ไฟล์เดียวกันถูกรันสองครั้งกับโค้ดสองเวอร์ชัน การทดสอบที่ "ผ่านทั้งใหม่และเก่า" ไม่นับเป็นความคืบหน้า **ในรอบขยาย**
(ตั้งแต่ Layer 06.6 กติกาข้อนี้เลิกเป็นกฎเดียวของระบบ — ในรอบยุบรวมมันคือสิ่งที่ *ต้องการ* ดูหัวข้อ 🧹 ด้านล่าง)
ดูโค้ดไฟล์พิสูจน์ของทุกรอบได้จากปุ่ม **🧾 ดูโค้ดไฟล์พิสูจน์** ใน Evolution Ledger — รอบที่ถูกตีตกก็ยังเก็บไฟล์ไว้เป็นหลักฐาน

**ราวกันตกที่บังคับใช้ด้วยโค้ด ไม่ใช่แค่คำสั่งในพรอมป์ต์:**

- `data/*.json` (ความทรงจำทั้งหมด) ถูกเทียบไบต์ต่อไบต์หลังทุกรอบ — ถ้าเปลี่ยน จะคืนค่าเดิมและตีตกทั้งรอบ ระบบจึงแต่งประวัติของตัวเองไม่ได้
- แก้ได้เฉพาะไฟล์ในโฟลเดอร์โปรเจกต์นี้ (`data/ lab/ inbox/ evolution/ .git/` ถูกกันออกจากขอบเขตการแก้)
- ทุกไฟล์ที่เปลี่ยนถูกสำรองก่อนเสมอ → ย้อนกลับได้ 100%
- โค้ดใหม่ต้องบูตเป็นเซิร์ฟเวอร์จริงและตอบ HTTP ได้ ไม่ใช่แค่ syntax ถูก
- **ต้องพิสูจน์ด้วยโค้ดว่าความสามารถใหม่ใช้ได้จริง และโค้ดเดิมทำสิ่งนั้นไม่ได้**
- **กฎ "ห้ามทำให้ความสามารถเดิมพัง" ถูกบังคับด้วย regression sweep ทุก endpoint ที่ยังมีชีวิต ไม่ใช่แค่ข้อความในพรอมป์ต์**
  (เส้นทางจะออกจาก sweep ได้ก็ต่อเมื่อบันทึกการใช้งานจริงยืนยันว่าถูกเรียก 0 ครั้ง — และการถอดถูกบันทึกไว้และกดคืนได้)
- การทดสอบทั้งหมดรันในแซนด์บ็อกซ์ (สำเนาโค้ด + สำเนาความทรงจำใน temp) — ทดสอบเท่าไหร่ก็ไม่แตะข้อมูลจริง
- ห้ามเพิ่ม dependency ภายนอก — ระบบต้องรันด้วย Node เปล่าตลอดไป
- **เพดานการลองของกำแพงหนึ่ง ๆ ไม่ใช่ค่าคงที่ 3 อีกต่อไป**: เริ่มที่ 3 และขยับขึ้นครั้งละ 1 (สูงสุด 6)
  ทุกครั้งที่รอบที่ตก "เสนอวิธีที่ต่างจากเดิมจริง" — ลองวิธีเดิมซ้ำสามครั้งยังตันเหมือนเดิม (กันวนลูปตาย)
- **ทุกรอบขยายต้องประกาศเส้นทางที่ความสามารถของมันจะถูกใช้ผ่าน** (ด่าน `declared_usage`) และคำประกาศนั้นถูกตรวจกับ router จริง —
  เส้นทางที่อ้างแต่ไม่มีอยู่จริงถูกทิ้งและบันทึกไว้ว่าอ้างเกิน · เส้นทางที่ diff เพิ่มเข้ามาจริงถูกใส่ให้เอง (อ้างน้อยได้ ปิดบังไม่ได้)
- **คะแนน "ถูกใช้จริง" คำนวณจากบันทึกที่ระบบเขียนเอง ไม่ใช่จากคำพูดของรอบนั้น** และรอบหลอมตัวเองถูกห้ามแก้กลไกนี้ให้อ่อนลง
  (`recordEndpointHit`, `loadUsage`, `declareAdoption`, `adoptionRound`, `adoptionReport`, `weightedSignal`, `targetRanking`)
- **กลไกที่ทำให้คลังความรู้ถูกพิสูจน์ได้ ห้ามถูกทำให้อ่อนลง** (`forgeKnowledge`, `wallRelevance`, `buildKnowledgeBlock`,
  `forgeFeedbackPlan`, `forgeFeedbackRecord`, `applyForgeFeedback`, `introspectKnowledgeBlock`) — โดยเฉพาะห้ามทำให้
  การประกาศ `used_dot_ids` กลายเป็นสิ่งที่ไม่มีผลกับคะแนนของจุด · การเขียนคำประกาศกลับเข้า `data/` ยังต้องเกิด
  **หลังคำตัดสินเท่านั้น** — ระหว่างด่าน มันถูกจับว่าแตะความทรงจำเหมือนเดิมทุกประการ
- ปิดวิวัฒนาการอัตโนมัติเมื่อไหร่ก็ได้ด้วย `EVOLVE_HOURS=0` · ปิดการป้อนกลับของคะแนนการถูกใช้ด้วย `ADOPTION_WEIGHT=0`

### 🧹 Layer 06.6 — รอบยุบรวม: ระบบที่ลดขนาดตัวเองได้โดยพิสูจน์ได้

ด่านความต่าง (`out.differential.ok = !oldProof.ok`) เข้มงวดถูกต้อง — "ผ่านทั้งคู่ = ไม่ได้ทำอะไรใหม่" —
โดยไม่มีใครสังเกตว่ามันเท่ากับ **สั่งห้ามการลดขนาดตัวเองไปด้วย**: การรีแฟกเตอร์ที่ดีย่อมผ่านทั้งสองเวอร์ชัน นั่นคือนิยามของมัน
บวกกับ `REGRESSION_ENDPOINTS` ที่มีแต่ต่อท้าย ระบบจึงมีทิศทางเดียวคือใหญ่ขึ้น **นี่คือกำแพงที่ผลิตกำแพงอื่น**

ตอนนี้มีรอบสองชนิด ที่ฟังก์ชันความเหมาะสมกลับด้านกันพอดี:

| | รอบขยาย (expansion) | รอบยุบรวม (consolidation) |
|---|---|---|
| ไฟล์พิสูจน์กับโค้ดใหม่ | ต้อง **ผ่าน** | ต้อง **ผ่าน** |
| ไฟล์พิสูจน์กับโค้ดเดิม | ต้อง **ตก** | ต้อง **ผ่าน** ← กลับด้าน |
| ชุดทดสอบสะสมทั้งชุด | ไม่ถูกรันซ้ำ | **รันซ้ำทั้งชุดกับทั้งสองเวอร์ชัน ผลต้องเหมือนกันทุกไฟล์** |
| เกณฑ์ความคืบหน้า | มีความสามารถใหม่ | **ตัวเลขขนาดลดลง และห้ามมีตัวไหนโตขึ้น** |
| regression sweep | บังคับครบทุกเส้น | บังคับครบทุกเส้น (เท่าเดิม) |
| สั่งได้ที่ | `POST /api/transcend` | `POST /api/consolidate` |

ตัวเลขที่นับว่า "เล็กลง": `code_lines` · `code_chars` · `code_files` · `prompt_chars` (ขนาดพรอมป์ตที่ระบบใช้อ่านตัวเอง = เพดานบริบทของมันเอง) · `endpoints`
ไฟล์ใน `selftest/` ถูกตัดออกจากการนับ เพราะทุกรอบ *ถูกบังคับ* ให้เพิ่มไฟล์พิสูจน์ — ถ้านับด้วย รอบยุบรวมก็จะเป็นไปไม่ได้ด้วยเหตุผลเดียวกับกำแพงเดิม

**รอบยุบรวมไม่ใช่ทางง่าย** — มันรันชุดทดสอบทั้งชุดกับสองเวอร์ชัน แล้วบังคับว่า *ทุกไฟล์ต้องให้ผลเหมือนเดิม*
ไฟล์เดียวพลิกผล = ย้อนกลับทั้งรอบ (รอบขยายบังคับให้ **หนึ่ง** ไฟล์ต่างกัน · รอบยุบรวมบังคับให้ **ทุก** ไฟล์เหมือนกัน)
และเมื่อสะสมรอบขยายครบ `CONSOLIDATE_EVERY` รอบ daemon จะสั่งรอบยุบรวมให้เองแทนรอบขยาย — การเติบโตกลายเป็นหนี้ที่ต้องจ่าย

### 🚪 ทะเบียนการถอดเส้นทาง: sweep ที่ถอดออกได้ ไม่ใช่แค่ต่อท้าย

```
ทุก request ที่ /api/* → บันทึกจำนวนครั้งลง evolution/endpoint-usage.json (นับในหน่วยความจำ เขียนลงดิสก์แบบหน่วง)
เส้นทางที่ถูกเรียก 0 ครั้งตั้งแต่เริ่มนับ  → "ถอดออกจาก sweep ได้" (retirable)
เส้นทางที่มีคนเรียกแม้ครั้งเดียว          → ถอดไม่ได้ ปฏิเสธพร้อมบอกจำนวนครั้งและเวลาล่าสุด
ถอดแล้ว → บันทึกลง evolution/deprecations.json ว่าใครถอด เมื่อไหร่ เพราะอะไร · กดคืนเข้า sweep ได้ทุกเมื่อ
          ประกาศเดิมใน REGRESSION_ENDPOINTS ไม่เคยถูกลบ — มันคือประวัติศาสตร์ ไม่ใช่รายการงาน
```

ทั้งสองไฟล์อยู่ **นอก `data/`** โดยตั้งใจ: `data/*.json` ถูกเทียบไบต์ต่อไบต์หลังทุกรอบหลอมตัวเอง
ถ้าบันทึกการใช้งานอยู่ใน `data/` การที่ผู้ใช้เปิดหน้าเว็บระหว่างรอบวิวัฒนาการจะกลายเป็น "การแตะความทรงจำ" และตีตกรอบที่ดีทิ้งทันที

รอบยุบรวมขอถอด endpoint ได้ผ่าน field `retire_endpoints` แต่ **ระบบตรวจซ้ำเองเสมอ** ด้วย `planRetirements()` —
คำขอที่เส้นทางยังมีคนเรียกจะถูกปฏิเสธและบันทึกไว้ในรอบนั้น และการถอดจะถูกเขียนลงทะเบียนก็ต่อเมื่อรอบนั้น *ผ่านทุกด่านแล้ว* เท่านั้น
ระหว่างการตรวจสอบ เส้นทางที่กำลังขอถอดจะถูกยกออกจาก sweep ชั่วคราว มิฉะนั้นรอบที่ลบโค้ดของมันจะตกด่าน regression ของตัวเองเสมอ (ไก่กับไข่)

`POST /api/consolidation/dryrun` เปิดให้เรียกฟังก์ชันความเหมาะสมกลับด้านนี้ด้วยตัวเลขสมมติได้โดยไม่เสียรอบ AI
ใช้ตรวจเองได้ว่ากรณีไหนผ่านกรณีไหนตก รวมถึงกรณีสำคัญที่สุด: **ลายเซ็นของรอบขยาย (ตกกับเก่า/ผ่านกับใหม่) ต้องถูกรอบยุบรวมปฏิเสธ**

### 🔁 Layer 06.7 — ลูปปิด: ผลตรวจที่ไปถึงคนที่ยังแก้ได้ทัน

รอบหลอมตัวเองเคยเป็น **การยิงนัดเดียวจบ** — และไม่มีใครสังเกตว่ามันคือกำแพงชนิดเดียวกับที่ Layer 06.5 กับ 06.6 แก้ไปแล้ว
เพียงแต่มันซ่อนอยู่ในแกนเวลาแทนที่จะซ่อนอยู่ในกติกา:

```
เดิม   runClaude(prompt, {tools: [Read, Edit, Write, Glob, Grep]})   ← ไม่มี Bash: รัน node --check ไม่ได้
       └─ โมเดลตอบ → โปรเซสตาย
          จากนั้นค่อยรัน: syntaxCheck → smokeTest → proveEvolution → regressionTest
          runProof() ได้ stdout จริงของไฟล์ที่โมเดลเพิ่งเขียนมาเต็ม ๆ
          แล้วเก็บมันไว้ใน checks.capability_detail — ฟิลด์ที่ "รอบนั้น" ไม่มีวันได้อ่าน
          ความรู้ไปถึงมือคนที่ยังแก้ได้เร็วที่สุดคือ *รอบถัดไป* ผ่าน scar tissue: ห่างออกไป 5–20 นาที และหนึ่งโควตา
```

ตอนนี้รอบเดียวกันมีได้หลายเทิร์น และผลตรวจกลายเป็นพรอมป์ตของเทิร์นถัดไปทันที:

```
          ask ──→ runForgeGates() ──ผ่านทุกด่าน──→ accepted
           ↑            │
           │        ตกที่ด่านไหน + stdout จริงของด่านนั้น (ไม่ย่อ)
           └── buildGateFeedback() ── เซสชันเดิม · รอบเดิม · โค้ดยังอยู่บนดิสก์ ยังไม่ถูกย้อนกลับ
```

| ชิ้นส่วน | หน้าที่ | ทำไมต้องแยกออกจากกัน |
|---|---|---|
| `runForgeGates()` | รันด่านทั้งชุดกับทรีที่อยู่บนดิสก์ตอนนั้น (guard → proof_written → syntax → smoke → 3 ด่านของโหมดนั้น) | **ไม่ย้อนกลับอะไรเลย** — การย้อนกลับเป็นการตัดสินใจของรอบ ที่เกิดครั้งเดียวตอนจบ ไม่ใช่ของด่าน |
| `buildGateFeedback()` | แปลงผลด่านเป็นพรอมป์ตเทิร์นถัดไป โดยพา stdout จริงไปทั้งดุ้น | ข้อความที่โมเดลได้อ่านต้องตรวจสอบได้จากภายนอกโดยไม่ต้องเสียรอบ AI |
| `forgeConverge()` | ตัวขับลูป: ถาม → ตรวจ → ป้อนกลับ → หยุด | `ask`/`verify` ถูกฉีดเข้ามา จึงรันตัวขับตัวเดียวกันนี้แบบออฟไลน์ได้ที่ `POST /api/forge/loop/simulate` |

**สิ่งที่ยังไม่เปลี่ยน (โดยตั้งใจ)** — ลูปนี้ไม่ได้ทำให้ผ่านง่ายขึ้นแม้แต่นิดเดียว:

- ด่านทุกด่านเหมือนเดิมเป๊ะ และถูกรัน **ซ้ำทั้งชุดทุกเทิร์น** ไม่ใช่รันเฉพาะด่านที่เคยตก
- `data/*.json` ถูกเทียบไบต์ต่อไบต์และคืนค่าเดิม **ทุกเทิร์น** ไม่ใช่ตอนจบรอบเท่านั้น
- ไฟล์พิสูจน์ยังต้องผ่านกับโค้ดใหม่และตกกับโค้ดเดิม (รอบขยาย) เหมือนเดิมทุกประการ
- พรอมป์ตที่ป้อนกลับ **สั่งห้ามแก้กลไกตรวจสอบให้อ่อนลง** และห้ามแก้ไฟล์พิสูจน์ให้หลวมลงเมื่อตกด่าน differential
- เทิร์นซ่อมที่ **เริ่มไม่ได้** (เซสชันหาย/หมดเวลา) ไม่ทำให้รอบแย่ลงกว่าเดิม: ระบบเก็บคำตัดสินของเทิร์นที่รันไปแล้ว
- ครบเพดานเทิร์นแล้วยังตก = ย้อนกลับทั้งรอบ และยังทิ้งแผลเป็นให้ Layer 06.5 เหมือนเดิม

**กำไรที่ได้จริง**: รอบที่ตกเพราะวงเล็บเกินหนึ่งตัว หรือเพราะไฟล์พิสูจน์เขียน assertion ผิดนิดเดียว
เคยต้องเสียทั้งรอบ (5–20 นาที) **และหนึ่งช่องโควตาของกำแพงนั้น** ทั้งที่ข้อมูลที่จะแก้มันมีอยู่แล้วตั้งแต่นาทีที่ 3
ตอนนี้มันถูกแก้ในรอบเดียวกัน และการ์ดใน Evolution Ledger จะขึ้นแถบ 🔁 บอกว่าเทิร์นไหนตกที่ด่านไหน
ถูกป้อนผลตรวจกลับกี่ตัวอักษร และเทิร์นถัดไปกู้กลับมาได้หรือไม่

```
FORGE_MAX_TURNS=1   ← กลับไปเป็นพฤติกรรมยิงนัดเดียวจบแบบเดิมเป๊ะ (ลูปยังอยู่ แต่ไม่มีเทิร์นที่สอง)
FORGE_MAX_TURNS=3   ← ค่าเริ่มต้น: เขียน 1 ครั้ง แก้ได้อีก 2 ครั้งจากผลตรวจจริง
```

`POST /api/forge/loop/simulate` เรียก `forgeConverge()` **ตัวจริง** ด้วยผลด่านสมมติ (ไม่ใช้ AI ไม่เขียนไฟล์)
จึงตรวจเองได้ว่า stdout ของเทิร์นที่ 1 ไปโผล่ในพรอมป์ตของเทิร์นที่ 2 จริงไหม ลูปหยุดทันทีที่ผ่านไหม และครบเพดานแล้วหยุดจริงไหม

### 🩹 แผลเป็น: รอบที่ล้มเหลวถูกส่งต่อ ไม่ใช่ถูกลบ

เดิมรอบที่ไม่ผ่านหายไปสองชั้น: `restoreBackup()` ลบโค้ดที่มันเขียนทิ้ง และ ledger ที่เหลือไว้เขียนไว้ให้ "มนุษย์อ่านย้อนหลัง"
ไม่มีเส้นทางไหนพามันกลับเข้าพรอมป์ตรอบถัดไป — `buildForgePrompt()` เห็นแค่ตัวขอบเขตกับซอร์สโค้ดปัจจุบัน
ระบบจึงเดินชนกำแพงเดิมด้วยวิธีเดิมได้ครบสามครั้งแล้วโดนตัดสิทธิ์ถาวร ทั้งที่ครั้งที่สองควรรู้แล้วว่าครั้งแรกพลาดตรงไหน

ตอนนี้ทุกรอบที่ตกทิ้งแผลเป็นไว้สามอย่าง:

| สิ่งที่เก็บ | เก็บที่ไหน | ใครใช้ |
|---|---|---|
| โค้ดที่เขียนแล้วไม่ผ่าน (ทุกไฟล์ที่แตะ) | `evolution/backups/<evo-id>/attempted/` | รอบถัดไปเปิดอ่านด้วย Read · ผู้ใช้กด **📦 ดูโค้ดที่รอบนี้ลองแล้วไม่ผ่าน** |
| ด่านที่ตก + ข้อความจริงของด่าน + ไฟล์ที่แตะ | `data/evolution.json` (`checks`, `files`, `report`) | ประกอบเป็นบล็อก "ประวัติความล้มเหลว" ที่หัวพรอมป์ตรอบถัดไป |
| ความต่างของแนวทาง (ชุดไฟล์ + คำอธิบาย) | คำนวณสด ๆ จาก ledger | ใช้ตัดสินว่าเพดานของกำแพงนี้ควรขยับหรือไม่ |

**เพดานที่ขยับได้ทำงานอย่างไร** — เทียบสองแนวทางว่า "เหมือนกัน" ก็ต่อเมื่อ *แตะชุดไฟล์เดียวกัน* และ *คำอธิบายซ้อนกันเกิน 60%*
(วัดด้วย Jaccard บน 4-gram ของตัวอักษร เพราะข้อความเป็นภาษาไทยที่ไม่มีช่องว่างให้ตัดคำ)

```
เพดาน = 3 + (จำนวนแนวทางที่ต่างกันจริง − 1)   สูงสุด 6
ตก 3 ครั้งด้วยวิธีเดียวกัน  → แนวทางต่าง 1 แบบ → เพดาน 3 → ตัน (ต้องคิดวิธีใหม่ก่อน)
ตก 3 ครั้งด้วย 3 วิธีจริง   → แนวทางต่าง 3 แบบ → เพดาน 5 → ยังไปต่อได้อีก 2 ครั้ง
```

กำแพงที่ตันไม่ได้ถูกลบความทรงจำ — ยังกดดูประวัติได้ และถ้ามีวิธีใหม่จริงเมื่อไหร่ เพดานก็ขยับให้เอง
ดูทั้งหมดได้จากปุ่ม **🩹 บทเรียนจาก N รอบที่ล้มเหลว** ในรายการขอบเขต ซึ่งแสดง *บล็อกข้อความจริง* ที่จะถูกส่งเข้าพรอมป์ตรอบถัดไป

## เปิดเครื่องแล้วรันเอง (แนะนำ)

รันคำสั่งนี้ครั้งเดียวเพื่อให้ Serendipity Engine เริ่มพร้อม Windows:

```powershell
Copy-Item "C:\Users\akara\Steve Jobs\dot-connector\DotConnectorAI.vbs" ([Environment]::GetFolderPath('Startup'))
```

ถอนออก: `powershell -File uninstall-autostart.ps1`

## วิธีรัน

ต้องมี Node.js และ Claude Code CLI ที่ล็อกอินแล้ว (ไม่ต้องมี API key)

```powershell
node server.js
```

แล้วเปิด **http://localhost:4747**

## วิธีใช้

1. **เพิ่มจุด** — ยิ่งต่างสาขากันมาก การเชื่อมยิ่งคาดไม่ถึง
2. (ไม่บังคับ) เลือกจุดเฉพาะ / พิมพ์โจทย์ที่อยากปลดล็อก แล้วกด **⚡ เชื่อมจุด** (1–4 นาที)
3. ได้นวัตกรรมแล้ว กด **🧪 พิสูจน์ไอเดียนี้** — Evidence Agent จะค้นเว็บทั่วโลก (3–8 นาที) แล้วรายงาน:
   - **Novelty verdict**: ยังไม่มีใครทำ / มีของใกล้เคียง / มีแล้ว — พร้อมรายชื่อสิ่งที่เจอจริง
   - **Feasibility** คะแนน + ความเสี่ยงใหญ่สุด + ทางแก้
   - **การทดลองที่เล็กที่สุด** พร้อมเกณฑ์วัดผล
   - **ต้นแบบรันได้จริง** บันทึกเป็นไฟล์ใน `lab/<connection-id>/` เปิดจากลิงก์ในหน้าเว็บได้เลย
4. **บันทึกผลลัพธ์จริง** ใต้ไอเดียแต่ละอัน (ให้คะแนน 1-5 + เลือกว่า เอาไปทำจริง / กำลังทดลอง / พักไว้ / ตายแล้ว + เขียนสั้น ๆ ว่าเกิดอะไรขึ้น)
   — นี่คือเส้นตอบกลับ: ระบบจะบอกทันทีว่าคะแนนคุณค่าของจุดไหนเพิ่งขยับขึ้นหรือลงเพราะผลนี้ และรอบเชื่อมจุดครั้งถัดไปจะถูกป้อนบทเรียนนี้แทนบัญชีดำรายชื่อ
5. **ให้คลังโตเองตอนคุณไม่ว่าง (Layer 07)** — แถบ 🔭 The Scout เหนือคลังจุดบอกที่มาของทุกจุด กด **🧪 กู้หลักฐานที่เคยค้นเจอ** เพื่อเปลี่ยน "สิ่งใกล้เคียงที่มีอยู่จริงในโลก" ที่ระบบเคยค้นเจอตอนพิสูจน์ไอเดีย (แล้วทิ้งทุกครั้ง) ให้กลายเป็นจุดที่เอาไปเชื่อมได้ · กด **🌐 ออกไปหาความรู้ใหม่เอง** หรือกดชิปโดเมนที่ยังว่าง เพื่อให้ระบบเขียนคำค้นของตัวเองแล้วค้นเว็บมาเติมสาขาที่คลังยังไม่มีตัวแทน · จุดที่ระบบหามาเองจะเป็นการ์ดเส้นประพร้อมปุ่ม **🚫** — กดปฏิเสธแล้วมันจะไม่กลับมาอีก
6. **ให้มันลดขนาดตัวเอง (Layer 06.6)** — ในส่วน "รอบยุบรวม" ใต้ Layer 06 จะเห็นน้ำหนักปัจจุบันของระบบ (บรรทัดโค้ด · ขนาดพรอมป์ตที่มันใช้อ่านตัวเอง · จำนวน endpoint ที่ยังต้อง sweep) และหนี้การเติบโตที่สะสมไว้ · กด **🧹 สั่งรอบยุบรวมตัวเอง** เพื่อให้มันรวบโค้ดซ้ำและลบของที่ไม่มีใครใช้ โดยต้องพิสูจน์ว่าพฤติกรรมไม่เปลี่ยนด้วยชุดทดสอบเดิมที่ต้องผ่านทั้งก่อนและหลัง · กด **🗂 ทะเบียนเส้นทาง** เพื่อดูว่า endpoint ไหนถูกเรียกจริงกี่ครั้ง และถอดเส้นทางที่ไม่มีใครเรียกเลยออกจาก regression sweep ได้ด้วยตัวเอง (กดคืนได้ทุกเมื่อ)
7. **ดูว่าของที่มันสร้างมีใครใช้จริงไหม (Layer 06.8)** — ในส่วน "ด่านที่มาทีหลัง" ใต้รอบยุบรวม แถบ 📈 บอกว่ารอบขยายกี่รอบที่ **ถูกใช้จริง** กี่รอบที่ **ไม่มีใครเรียกเลย** และกี่รอบที่ยังรอเวลาตัดสิน · กด **📈 ดูทุกรอบและเส้นทางที่มันประกาศไว้** เพื่อเห็นทีละรอบว่ามันสัญญาว่าจะถูกเรียกผ่านเส้นทางไหน และเส้นทางนั้นถูกเรียกไปกี่ครั้งจริง · กด **⚖ ลำดับกำแพงถัดไป** เพื่อเห็นการป้อนกลับด้วยตาตัวเอง: คะแนนที่ประเมินไว้ก่อนลงมือ ถูกถ่วงด้วยสิ่งที่เกิดขึ้นจริงกับของที่เคยสร้างในหมวดเดียวกัน จนลำดับเปลี่ยน
8. **ดูว่ามันใช้ความรู้ของตัวเองตอนเขียนโค้ดตัวเองไหม (Layer 06.9)** — ในส่วน "เครื่องมือเชื่อมจุด ใช้จุดของตัวเองตอนเขียนโค้ดตัวเอง" กด **🧠 ดูความรู้ที่รอบถัดไปจะได้อ่าน** เพื่อเห็นบล็อกจริงที่จะถูกยัดเข้าหัวพรอมป์ต: จุดไหนถูกเลือกเพราะอะไร (`ความเกี่ยว` = ทาบโครงสร้างตรงกัน × 3 + ลำดับความสนใจ + ประวัติการช่วยทำลายกำแพง), นวัตกรรมที่ระบบเคยสังเคราะห์เอง, และ **ตารางคะแนนว่าจุดไหนเคยช่วยทำลายกำแพงได้จริง / จุดไหนถูกอ้างแล้วกำแพงไม่ล้ม** · กด **⚡ สั่งรอบเชื่อมจุดเล็งกำแพงเป้าหมาย** (หรือปุ่ม ⚡ บนกำแพงแต่ละอัน) เพื่อให้ระบบเอาเครื่องมือเชื่อมจุดของตัวเองมาเล็งกำแพงนั้นโดยตั้งโจทย์จากคำอธิบายกำแพง + เหตุผลที่มันยังอยู่ · กด **🧪 ลองด่านป้อนกลับด้วยรอบสมมติ** เพื่อเห็นด้วยตาว่าถ้ารอบหนึ่งอ้างว่าใช้จุดเหล่านี้แล้วผ่าน/ตก คะแนนของจุดนั้นจะขยับไปทางไหน (ไม่เสียรอบ AI ไม่แตะคลัง)
9. จุดที่ถูกทิ้งนานจะขึ้นแบนเนอร์ Serendipity ให้กด **⚡ ปลุกจุดนี้** (เรียงตามความสนใจที่ถ่วงด้วยคุณค่าแล้ว จุดที่เคยพาไปทางตันซ้ำ ๆ จะไม่ถูกดันขึ้นมาก่อน)

### เส้นตอบกลับทำงานอย่างไร (Layer 04.5)

```
ไอเดีย → Evidence verdict (world_first +1 · similar_exists +0.25 · already_exists −1)
       → ผู้ใช้ให้คะแนนผลจริง (rating 1-5 และ shipped +1 · testing +0.4 · parked −0.2 · dead −1, น้ำหนักเป็น 2 เท่าของ verdict)
       → รวมเป็น "สัญญาณ" ของการเชื่อมนั้น ในช่วง [−1, +1]
       → เฉลี่ยลงบนทุกจุดที่อยู่ในการเชื่อมนั้น = value_score ของจุด
       → attention_score = (rarity + 1) × (1 + value_score)   ← ใช้จัดลำดับแทน rarity ล้วน
       → จุดถูกส่งเข้าพรอมป์ตรอบถัดไปโดยเรียงตาม attention_score พร้อมบทเรียน
          "✅ ทิศทางที่ได้ผล / ❌ ทิศทางที่ตายแล้ว / ◻ ผลกลาง ๆ" แทนรายชื่อห้ามซ้ำ
```

จุดที่ยังไม่มีผลตอบกลับได้ `value_score = 0` — ลำดับเดิมจึงไม่ถูกรบกวนจนกว่าจะมีหลักฐานจริง
ทุกรอบการเชื่อมยังบันทึกด้วยว่า AI สรุปบทเรียนรอบก่อนไว้ว่าอย่างไร (`learned_from_last_round`) จึงตรวจสอบย้อนหลังได้ว่าเส้นตอบกลับมีผลจริง

## API

| Endpoint | ความหมาย |
|---|---|
| `GET/POST/DELETE /api/dots` | จัดการคลังจุด (GET คืนค่าพร้อมสถิติ HexKern: uses, rarity, forgotten และ **ป้ายที่มา `origin` / `origin_label` / `self_sourced` ของ Layer 07**) · DELETE จุดที่ระบบหามาเองนับเป็นการยับยั้งด้วย |
| `POST /api/capture` | โยนความคิดดิบ — body: `{text}` → AI สกัดเป็นจุด |
| `GET /api/forgotten` | จุดที่กำลังจะถูกลืม (เรียงตาม rarity) |
| `POST /api/connect` | เชื่อมจุด — body: `{focus?, dotIds?, mustInclude?}` |
| `POST /api/evidence` | พิสูจน์ไอเดีย — body: `{connectionId}` (ใช้ web search จริง) |
| `GET /api/connections` | ประวัตินวัตกรรมทั้งหมด |
| `POST /api/outcome` | **บันทึกผลลัพธ์จริงของไอเดีย** — body: `{connectionId, rating: 1-5, status: shipped\|testing\|parked\|dead, note?}` → คืนค่าจุดที่คะแนนเพิ่งขยับเพราะผลนี้ |
| `GET /api/lessons` | **บทเรียนที่รอบถัดไปจะถูกป้อน** — อะไรได้ผลจริง อะไรตายแล้ว พร้อมสรุปตัวเลข |
| `GET /api/self/runtime` | **Layer 11: บันทึกการเดินเครื่องจริง** — รอบที่ตกและตกด่านไหน · เทิร์นซ่อม · เส้นทางที่ไม่มีใครเรียก · บันทึก daemon (`?days=`) |
| `GET /api/introspect/preview` | พรอมป์ตส่องกระจกทั้งก้อน ประกอบแล้วแต่ไม่ส่ง — พร้อมหลักฐานว่าบล็อกพฤติกรรมอยู่ในนั้นจริง |
| `GET /api/forge/bundle` | **Layer 12: ซอร์สที่รอบถัดไปจะได้อ่านจริง + งบ** (`?limitId=` `?budget=` `?focusChars=`) |
| `GET /api/judge` | **Layer 13: สัญญาการตรวจที่แช่แข็งไว้นอกมือของผู้ถูกตัดสิน** + ตอนนี้ซอร์สยังเคารพมันอยู่ไหม |
| `POST /api/forge/blind/simulate` | ขับผู้ออกข้อสอบตาบอดด้วยตัวปลอม — พิสูจน์ว่ามันไม่เห็น diff |
| `GET /api/restart/status` | **Layer 14: ตัวที่รันอยู่คือโค้ดบนดิสก์หรือยัง** + ประวัติการรีสตาร์ตเองทุกครั้งพร้อมผลลัพธ์ |
| `GET /api/forge/population` | **Layer 15: ประชากรต่อรอบ + แนวทางที่ผู้สมัครแต่ละคนถูกบังคับให้เดิน** |
| `POST /api/forge/population/simulate` | ตรรกะการเลือกผู้ชนะ ด้วยผู้สมัครสมมติ |
| `POST /api/forge/population/dryrun` | การแข่งจริงทั้งกระบวน (ทรีจริง บูตจริง) โดยแทนเฉพาะเซสชัน AI ด้วยตัวปลอม |
| `GET /api/limits/events` | **Layer 10: บัญชีสะสมของทะเบียนกำแพง** — ทุกการเปลี่ยนสถานะที่เคยเกิด (`?limitId=` เพื่อดูของข้อเดียว) · เขียนทับไม่ได้ |
| `POST /api/limits/merge/dryrun` | **เรียกกลไกรวมทะเบียนด้วยข้อมูลสมมติ** — body: `{current?, incoming}` → ผลลัพธ์ + เหตุการณ์ทั้งหมด โดยไม่แตะทะเบียนจริง |
| `GET /api/autopilot` | **Layer 09: สถานะสวิตช์กุญแจ** — สวิตช์ทุกตัว + `effective` (หลังคิดเพดานจาก env) + `locked` + `due_in_h` ของแต่ละความสามารถ |
| `POST /api/autopilot` | **เปลี่ยนสวิตช์** — body: `{master?, connect?, scout?, distill?, evolve?}` (boolean เท่านั้น มิฉะนั้น 400) |
| `POST /api/autopilot/stop` | **🛑 หยุดฉุกเฉิน** — ปิดสวิตช์ใหญ่ + ฆ่าโปรเซส AI ที่กำลังทำงานอยู่ทั้งหมด |
| `GET /api/scout/status` | **ที่มาของความรู้ทั้งคลัง** — จำนวนจุดแยกตาม origin, จำนวนที่ระบบหามาเอง, หลักฐานที่ยังรอกู้, ช่องว่างของโดเมน, คำค้นถัดไปที่ระบบตั้งเอง, รายการที่ถูกปฏิเสธ |
| `GET /api/scout/candidates` | **สิ่งที่ระบบเคยค้นเจอแล้วทิ้ง** — พรีวิว `similar_found` ทุกชิ้นที่ยังไม่ได้เป็นจุด (ไม่เขียนอะไรลงคลัง) |
| `GET /api/scout/gaps` | สำมะโนโดเมนของคลัง + โดเมนที่ไม่มีตัวแทนเลย + **คำค้นที่ระบบเขียนให้ตัวเอง** ของแต่ละช่องว่าง |
| `POST /api/scout/harvest` | **กู้หลักฐานเป็นจุดจริง** — body: `{max?}` · ไม่ใช้ AI ไม่ออกเน็ต · รันซ้ำได้ไม่เกิดจุดซ้ำ |
| `POST /api/scout/web` | **ให้ระบบออกไปค้นเว็บเอง** — body: `{domain?}` (ไม่ระบุ = เลือกช่องว่างอันดับหนึ่งเอง) |
| `POST /api/scout/reject` | **สิทธิ์ยับยั้งของเจ้าของ** — body: `{dotId}` → ลบจุดและจำคีย์ไว้ ระบบหยิบกลับมาไม่ได้อีก |
| `GET /api/knowledge/preview` | **Layer 08: ระบบชั่งน้ำหนักคลังความรู้ของตัวเอง** — จำนวนจุด, ความยาวจริงของ `buildConnectPrompt`, จุดที่แสดงเต็ม/ย่อเป็นดัชนี, `unbudgeted_chars` (ถ้าไม่มีเพดานจะกินเท่าไหร่), กลุ่มจุดที่ซ้ำซ้อนที่ระบบหาเจอเอง (ไม่ใช้ AI), จุดที่ proven ซึ่งยุบรวมไม่ได้, ประวัติรอบยุบรวม |
| `POST /api/knowledge/dryrun` | **เรียกฟังก์ชันความเหมาะสมของความรู้ด้วยแผนสมมติ** — body: `{clusters:[{dot_ids, title, domain, content}]}` หรือ `{before, after, connections?}` → บอกว่าผ่าน/ตกที่ด่านไหนเพราะอะไร · ไม่ใช้ AI ไม่แตะคลัง |
| `POST /api/knowledge/merge` | **ยุบรวมด้วยแผนที่เขียนเอง** — body: `{clusters, note?}` · ด่านเดียวกันทุกด่าน · ตกด่าน = ไม่มีจุดใดขยับ (HTTP 400) |
| `POST /api/knowledge/distill` | **สั่งรอบยุบรวมความรู้** — body: `{note?}` · AI เสนอแผน ระบบตัดสินด้วยตัวเลข |
| `GET /api/knowledge/archive` | **คลังจุดเดิมที่ถูกยุบรวม** — ทุกรอบพร้อมเหตุผลและตัวเลข · ใส่ `?roundId=` เพื่ออ่านจุดเดิมทั้งดวง |
| `POST /api/knowledge/restore` | **ย้อนรอบยุบรวมความรู้** — body: `{roundId}` → คืนจุดเดิมครบทุกตัวอักษร ถอนจุดหลักการออก |
| `POST /api/serendipity/scan` | สั่งรอบ daemon ทันที — body: `{forceConnect?}` |
| `GET /api/serendipity/status` | สถานะ daemon + log ล่าสุด |
| `GET /api/self` | ระบบมองเห็นตัวเอง: รายชื่อไฟล์/จำนวนบรรทัดของซอร์สตัวเอง + สถานะ forge |
| `GET /api/limits` | ทะเบียนขอบเขตของตัวเอง (ทำลายแล้ว/ยังยืนอยู่) + **งบการลองของแต่ละกำแพง**: `attempt_cap`, `attempts_left`, `cap_earned`, `distinct_approaches`, `exhausted`, `failed_attempts[]` |
| `POST /api/introspect` | สั่งให้ระบบอ่านตัวเองแล้วอัปเดตทะเบียนขอบเขต |
| `POST /api/transcend` | **ให้ระบบเขียนโค้ดตัวเองใหม่เพื่อทำลายขอบเขต** (รอบขยาย) — body: `{limitId?}` |
| `GET /api/consolidation/preview` | **Layer 06.6: ระบบชั่งน้ำหนักตัวเอง** — เมตริกขนาด (`code_lines`, `code_chars`, `prompt_chars`, `endpoints`), ชุดทดสอบสะสมที่ต้องผ่านเหมือนเดิมทุกไฟล์, เส้นทางที่ถอดได้, หนี้การเติบโต (`rounds_since_consolidation`/`due`) |
| `POST /api/consolidation/dryrun` | **เรียกฟังก์ชันความเหมาะสมกลับด้านด้วยตัวเลขสมมติ** — body: `{before, after, suite:[{file, own, old_ok, new_ok}], retiring?}` → บอกว่าผ่าน/ตกที่ด่านไหนเพราะอะไร · ไม่ใช้ AI ไม่เขียนอะไรลงคลัง |
| `POST /api/consolidate` | **สั่งรอบยุบรวม** — body: `{note?}` · ผ่านเมื่อชุดทดสอบเดิมผ่านทั้งก่อนและหลัง + ขนาดลดลง + regression ครบ |
| `GET /api/endpoints` | **ทะเบียนเส้นทาง + บันทึกการใช้งานจริง** — ใครถูกเรียกกี่ครั้ง ล่าสุดเมื่อไหร่ ใครถอดได้ ใครถูกถอดไปแล้ว |
| `GET /api/adoption` | **Layer 06.8: ของที่ระบบสร้างไว้ มีใครใช้บ้าง** — ทุกรอบขยายพร้อมเส้นทางที่มันประกาศไว้, จำนวนครั้งที่ถูกเรียกจริง, สถานะ (`adopted`/`unused`/`pending`/`undeclared`), คะแนนของแต่ละรอบและของทั้งระบบ, รอบที่ 0 hit ติดกัน (`unused_streak`), เป้าหมายที่เสนอให้รอบยุบรวม (`consolidation_targets`) และ **ลำดับกำแพงถัดไปหลังถ่วงด้วยคะแนนนี้ (`ranking`)** |
| `POST /api/adoption/dryrun` | **เรียกฟังก์ชันให้คะแนนการถูกใช้ตัวจริงด้วยบันทึกและนาฬิกาสมมติ** — body: `{rounds, usage:{started_at, endpoints}, now?, limits?}` → บอกทุกรอบว่าถูกนับเป็นอะไรเพราะอะไร และถ้าส่ง `limits` มาด้วยจะคืนลำดับกำแพงที่ถูกถ่วงแล้ว · ไม่ใช้ AI ไม่เขียนอะไรลงคลัง ไม่ต้องรอ `ADOPTION_DAYS` วัน |
| `POST /api/endpoints/retire` | **ถอดเส้นทางออกจาก regression sweep** — body: `{endpoint, reason?}` · ยอมเฉพาะเส้นทางที่ถูกเรียก 0 ครั้ง |
| `POST /api/endpoints/restore` | คืนเส้นทางที่ถอดไปแล้วกลับเข้า sweep — body: `{endpoint}` |
| `GET /api/forge/preview?limitId=&full=` | **พรอมป์ตจริงที่รอบถัดไปจะได้อ่าน** — พร้อมประวัติความล้มเหลวแบบมีโครงสร้าง (`history`), บล็อกบทเรียน (`failure_block`), งบการลอง (`budget`) · ไม่ใส่ `limitId` = ดูกำแพงที่เป็นเป้าหมายถัดไป · `full=1` = ขอพรอมป์ตเต็มรวมซอร์สโค้ด |
| `GET /api/forge/loop` | **Layer 06.7: ลูปปิดของรอบหลอมตัวเอง** — เพดานเทิร์นต่อรอบ (`max_turns`), เวลาของเทิร์นแรก/เทิร์นซ่อม, เครื่องมือที่โมเดลได้รับจริง (`tools` รวม `Bash(node --check:*)`), รายชื่อด่านที่ป้อนกลับได้ และประวัติเทิร์นของรอบที่ผ่านมา (`repaired_rounds` = รอบที่เคยตกแล้วกู้กลับได้ในรอบเดียว) |
| `POST /api/forge/loop/simulate` | **เรียกตัวขับลูปตัวจริงด้วยผลด่านสมมติ** — body: `{max_turns?, mode?, turns:[{failed_gates:[...], detail}]}` → คืน transcript ว่าเทิร์นไหนได้อ่านข้อความอะไรกลับไปบ้าง หยุดเพราะอะไร · ไม่ใช้ AI ไม่ออกเน็ต ไม่เขียนอะไรลงคลัง |
| `GET /api/forge/knowledge?limitId=` | **Layer 06.9: ความรู้ที่รอบหลอมตัวเองจะได้อ่านจริง** — จุดที่เกี่ยวกับกำแพงนั้นที่สุดพร้อม **เนื้อหาจริง** และคะแนนความเกี่ยว (`relevance`/`wall_overlap`), นวัตกรรมที่ระบบเคยสังเคราะห์พร้อม `hidden_pattern`, บทเรียน, รอบเชื่อมจุดที่เล็งกำแพงนี้, `block` (ข้อความจริงที่จะถูกยัดเข้าหัวพรอมป์ต), `introspect_knowledge` (บล็อกเดียวกันของรอบส่องกระจก), `dot_scoreboard` (จุดไหนช่วยทำลายกำแพงได้จริง) และ `feedback` (รอบไหนประกาศว่าใช้จุดไหน / รอบไหนไม่ประกาศ) |
| `POST /api/forge/knowledge/dryrun` | **เรียกฟังก์ชันเลือกความรู้และด่านป้อนกลับตัวจริงด้วยของสมมติ** — body: `{limit?, dots?, connections?, round?}` → `ranking` (ทุกจุดพร้อมเหตุผลที่ได้คะแนนเท่านั้น), `brief`, `block` และถ้าส่ง `round` มาด้วยจะคืน `feedback.moves` ว่าการประกาศ `used_dot_ids` ทำให้ `value_score`/`forge_score`/`attention_score` ของจุดนั้นขยับไปทางไหน · ไม่ใช้ AI ไม่แตะคลัง |
| `POST /api/forge/insight` | **เอาเครื่องมือเชื่อมจุดมาเล็งกำแพงของตัวเอง** — body: `{limitId?}` · รันรอบเชื่อมจุดจริงโดยตั้ง `focus = description + why_it_stands` ของกำแพง แล้วเก็บเป็นการเชื่อมจุดปกติ (ให้คะแนนผลลัพธ์ได้เหมือนไอเดียอื่น) เพื่อให้รอบหลอมตัวเองครั้งถัดไปได้อ่าน `hidden_pattern` ของมันในหัวพรอมป์ต |
| `GET /api/evolution` | บันทึกวิวัฒนาการทุกรอบ พร้อมผลตรวจ 4 ด่าน (syntax/boot/ความสามารถ/regression) ไฟล์ที่เปลี่ยน และบทเรียนที่รอบนั้นถูกป้อน (`history_used`) |
| `GET /api/evolution/proof?evoId=` | โค้ดไฟล์พิสูจน์ของรอบนั้น (รอบที่ถูกย้อนกลับก็ยังอ่านได้จาก backup) |
| `GET /api/evolution/attempted?evoId=&file=` | **โค้ดที่รอบซึ่งไม่ผ่านเคยเขียนไว้** — ไม่ใส่ `evoId` = รายชื่อทุกรอบที่ยังเก็บไว้ · ใส่ `file=` = อ่านทั้งไฟล์ |
| `POST /api/evolution/rollback` | ย้อนโค้ดกลับ — body: `{evoId}` |
| `POST /api/restart` | ให้เครื่องยนต์รีสตาร์ตตัวเองเข้าสู่โค้ดใหม่ |

## ตั้งค่า

| ตัวแปร | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `PORT` | `4747` | พอร์ตของเว็บ |
| `DOT_MODEL` | `claude-opus-5` | โมเดลเชื่อมจุด — ตั้งเป็น `sonnet` ถ้าอยากได้เร็ว/ประหยัดขึ้น |
| `HARVEST_MODEL` | `claude-opus-5` | โมเดลเก็บเกี่ยวโน้ตเป็นจุด |
| `FORGE_MODEL` | `claude-opus-5[1m]` | โมเดลที่อ่านและเขียนซอร์สโค้ดของตัวเอง (ต้องการ context ใหญ่) |
| `FORGE_BUNDLE` | `index` | `index` = ส่ง "แผนที่สัญลักษณ์" ให้ตัวหลอมแล้วให้มันใช้ Read เปิดอ่านเองเฉพาะที่ต้องแก้ (เล็กกว่าเดิม ~98%) · `full` = ยัดซอร์สทั้งก้อนแบบรอบ 1-11 ไว้เทียบคุณภาพ |
| `FORGET_DAYS` | `14` | จำนวนวันก่อนจุดถูกนับว่า "กำลังจะถูกลืม" |
| `CHECK_MIN` | `30` | daemon ตรวจ inbox/จุดถูกลืมทุกกี่นาที |
| `AUTO_HOURS` | `24` | เว้นอย่างน้อยกี่ชั่วโมงระหว่างการเชื่อมอัตโนมัติ (คุมค่าใช้จ่าย) |
| `EVOLVE_HOURS` | `24` | เว้นกี่ชั่วโมงระหว่างการวิวัฒนาการตัวเองอัตโนมัติ — **ใส่ `0` เพื่อปิด** (ยังกดจากหน้าเว็บเองได้) |
| `FORGE_MAX_TURNS` | `3` | Layer 06.7: หนึ่งรอบหลอมตัวเองมีได้กี่เทิร์น — เทิร์นแรกเขียน เทิร์นที่เหลือคือการแก้จาก stdout จริงของด่านที่ตก · **ใส่ `1` เพื่อกลับไปเป็นการยิงนัดเดียวจบแบบเดิม** (สูงสุดที่ยอมรับใน dry-run คือ 6) |
| `FORGE_TIMEOUT_MIN` | `45` | เวลาสูงสุดของเทิร์นแรกในหนึ่งรอบ (เทิร์นซ่อมได้ 60% ของค่านี้) |
| `CONSOLIDATE_EVERY` | `3` | Layer 06.6: สะสมรอบขยายที่ผ่านได้กี่รอบก่อนที่ daemon จะสั่ง **รอบยุบรวม** แทน — **ใส่ `0` เพื่อปิดการตั้งเวลาเอง** (ปุ่มบนหน้าเว็บยังใช้ได้) |
| `ADOPTION_DAYS` | `7` | Layer 06.8: หลังรอบผ่านไปกี่วัน "ไม่มีใครเรียก" จึงเริ่มนับเป็นคำตอบ (ก่อนหน้านั้นให้คะแนน `null`) |
| `ADOPTION_MIN_REQUESTS` | `50` | Layer 06.8: บันทึกต้องเห็นคำขอ `/api/*` ครบกี่ครั้งก่อน จึงจะตีความ "0 hit" ว่าไม่มีใครใช้ — กันการตัดสินสัปดาห์ที่ไม่มีใครเปิดหน้าเว็บเลย |
| `ADOPTION_FLUENT_HITS` | `10` | Layer 06.8: ถูกเรียกกี่ครั้งจึงนับว่า "ใช้จริงจัง" ไม่ใช่แค่แวะครั้งเดียว (ใช้ถ่วงคะแนนให้เป็นสเกล ไม่ใช่แค่ 0/1) |
| `ADOPTION_STREAK` | `2` | Layer 06.8: รอบที่ 0 hit ติดกันกี่รอบ ก่อนที่ daemon จะสั่ง **รอบยุบรวมเอง** โดยส่งรอบเหล่านั้นเป็นเป้าหมายไปให้ |
| `ADOPTION_WEIGHT` | `2` | Layer 06.8: คะแนนการถูกใช้ของหมวดถ่วงลำดับความคุ้มของกำแพงได้มากสุดกี่แต้ม — **ใส่ `0` เพื่อปิดการป้อนกลับเข้า `selectTarget()`** (การให้คะแนนยังทำงานอยู่) |
| `FORGE_KNOWLEDGE_DOTS` | `8` | Layer 06.9: จุดความรู้กี่จุดที่ถูกยัดเข้าหัวพรอมป์ตหลอมตัวเองพร้อมเนื้อหาจริง — **นี่คือเพดานที่ทำให้บล็อกความรู้ไม่โตตามคลัง** (`0` = ปิดการส่งจุดเข้าไป) |
| `FORGE_KNOWLEDGE_CHARS` | `360` | Layer 06.9: เนื้อหาของหนึ่งจุดกินพรอมป์ตหลอมตัวเองได้กี่ตัวอักษรก่อนถูกตัด |
| `FORGE_KNOWLEDGE_PER_DOMAIN` | `2` | Layer 06.9: จุดจากโดเมนเดียวกันเข้าบล็อกได้ไม่เกินกี่จุด — กันไม่ให้บล็อกกลายเป็นโดเมนเดียว ซึ่งจะทำให้การทาบข้ามโดเมนเป็นไปไม่ได้ |
| `FORGE_KNOWLEDGE_INNOVATIONS` | `4` | Layer 06.9: นวัตกรรมที่ระบบเคยสังเคราะห์เองกี่ชิ้น (เรียงตามสัญญาณจากเส้นตอบกลับ) ที่ถูกส่งไปพร้อม `hidden_pattern` ของมัน |
| `FORGE_KNOWLEDGE_LESSONS` | `4` | Layer 06.9: บทเรียนจากเส้นตอบกลับกี่ข้อที่ถูกส่งเข้าพรอมป์ตหลอมตัวเอง |
| `FORGE_INSIGHT_HOURS` | `72` | Layer 06.9: รอบเชื่อมจุดที่เล็งกำแพงหนึ่ง ๆ ถือว่า "ยังสด" กี่ชั่วโมงก่อนที่รอบหลอมตัวเองจะรันใหม่ให้ — **ใส่ `0` เพื่อไม่ให้รันเองเลย** (ปุ่มบนหน้าเว็บและรอบที่เก็บไว้แล้วยังใช้ได้) |
| `CONSOLIDATE_SUITE_MAX` | `8` | จำนวนไฟล์พิสูจน์สูงสุดที่รอบยุบรวมหนึ่งรอบจะรันซ้ำกับทั้งสองเวอร์ชัน (ไฟล์ที่ถูกข้ามจะถูกรายงานชื่อออกมาเสมอ ไม่ตัดเงียบ) |
| `SCOUT_HOURS` | `12` | Layer 07: เว้นกี่ชั่วโมงระหว่างที่ระบบออกไปค้นเว็บหาความรู้เอง — **ใส่ `0` เพื่อปิดการออกเน็ตเอง** (การกู้หลักฐานยังทำงานปกติ) |
| `SCOUT_MODEL` | ตาม `HARVEST_MODEL` | โมเดลที่ The Scout ใช้ค้นเว็บและสกัดเป็นจุด |
| `SCOUT_MAX` | `4` | จำนวนจุดสูงสุดที่ The Scout เก็บกลับมาได้ต่อการออกค้นหนึ่งครั้ง |
| `CONNECT_FULL_DOTS` | `40` | Layer 08: จุดกี่จุดแรก (เรียงตาม attention) ที่ถูกใส่ในพรอมป์ตเชื่อมจุดแบบเต็ม — ที่เหลือย่อเหลือดัชนีบรรทัดเดียว **นี่คือเพดานที่ทำให้พรอมป์ตเลิกโตเชิงเส้น** |
| `CONNECT_DOT_CHARS` | `420` | Layer 08: เนื้อหาของหนึ่งจุดกินพรอมป์ตได้กี่ตัวอักษรก่อนถูกตัด |
| `DISTILL_HOURS` | `24` | Layer 08: เว้นกี่ชั่วโมงระหว่างรอบยุบรวมความรู้อัตโนมัติ — **ใส่ `0` เพื่อปิด** (ปุ่มบนหน้าเว็บยังใช้ได้) |
| `DISTILL_SIMILARITY` | `0.42` | Layer 08: ความคล้ายของ 4-gram ที่ถือว่าสองจุดเป็นเรื่องเดียวกัน (สูง = ยุบรวมน้อยลงแต่มั่นใจกว่า) |
| `DISTILL_SCAN_MAX` | `260` | Layer 08: จุดล่าสุดกี่จุดที่ถูกนำมาเทียบกันแบบคู่ต่อคู่ (การหาคลัสเตอร์เป็น O(n²)) |
| `DISTILL_MODEL` | ตาม `HARVEST_MODEL` | Layer 08: โมเดลที่เขียน "จุดหลักการ" มาแทนกลุ่มจุดที่ซ้ำซ้อน |
| `DOT_SELFTEST` | — | ใช้ภายในตอนบูตทดสอบ: บูตเว็บได้แต่ไม่เริ่ม daemon และไม่เรียก AI |

## โครงสร้างไฟล์

```
dot-connector/
├── server.js               เซิร์ฟเวอร์ + Serendipity Daemon + Self-Forge (Node.js ไม่มี dependency)
├── public/index.html       หน้าเว็บ UI
├── DotConnectorAI.vbs      ตัวรันเงียบตอนเปิดเครื่อง (คัดลอกไป Startup folder)
├── uninstall-autostart.ps1 ถอนออกจาก Startup
├── inbox/                  Layer 00: โยนโน้ต .txt/.md ที่นี่ (processed/ = ที่เก็บไฟล์ที่อ่านแล้ว)
├── data/                   ⛔ ความทรงจำ — Self-Forge ห้ามแตะ (บังคับด้วยโค้ด)
│   ├── dots.json           Layer 01: คลังจุดความรู้
│   ├── connections.json    Layer 04: นวัตกรรม + หลักฐานการพิสูจน์ + ผลลัพธ์จริง (outcome) ที่ป้อนกลับเข้ารอบถัดไป
│   │                       Layer 06.9: `wall` = รอบเชื่อมจุดที่ถูกเล็งไปที่กำแพงของระบบเอง ·
│   │                       `forge` = คำประกาศของรอบหลอมตัวเองว่าใช้หลักการจากจุดไหน (outcome ของมันถูกให้คะแนนโดยด่าน ไม่ใช่มนุษย์)
│   ├── serendipity.json    Layer 05: สถานะ daemon + log · Layer 07: สถานะ The Scout (`scout.rejected` = คำยับยั้งของเจ้าของ, คำค้นที่เคยตั้งเอง)
│   ├── limits.json         Layer 06: ทะเบียนขอบเขตของตัวเอง
│   ├── evolution.json      Layer 06: บันทึกวิวัฒนาการทุกรอบ · Layer 06.8: `adoption` = เส้นทางที่รอบนั้นประกาศว่าจะถูกใช้ผ่าน
│   │                       + `adoption_used` = คะแนนการถูกใช้ที่มีผลตอนเลือกกำแพงนั้น (ตรวจย้อนหลังได้ว่าลูปเปลี่ยนการตัดสินใจจริง)
│   └── knowledge.json      Layer 08: บันทึกรอบยุบรวมความรู้ + จุดเดิมทั้งดวงที่ถูกยุบรวม (ย้อนกลับได้)
├── lab/<conn-id>/          ต้นแบบที่ Evidence Agent เขียน (รันได้จริง)
├── selftest/<evo-id>.js    Layer 06: ไฟล์พิสูจน์ความสามารถใหม่ของแต่ละรอบ (รันได้จริง เก็บสะสมเป็นชุดทดสอบของระบบ)
├── verifier/audit.js       ⛔ ผู้ตรวจอิสระ — Self-Forge ห้ามแตะ (อยู่นอก walkSelf + ถูกเทียบไบต์ต่อไบต์เหมือน data/)
│                           ไม่ใช้โค้ดร่วมกับ server.js เลยโดยเจตนา: ด่านของระบบเขียนโดยโมเดลตัวเดียวกับที่มันตัดสิน
│                           ความเห็นที่สองจึงมีค่าเฉพาะเมื่อผู้ถูกตรวจแก้ผู้ตรวจไม่ได้
│                           รัน: `node verifier/audit.js [--json]` → บูตแยกทุกไฟล์ รันชุดทดสอบสะสมทั้งหมด
│                           exit 0 = ผ่านหมด · 1 = มีข้อสอบเน่า (ความสามารถเก่าผุโดยไม่มีใครรู้)
├── evolution/endpoint-usage.json   Layer 06.6: บันทึกว่าเส้นทาง /api/* ไหนถูกเรียกจริงกี่ครั้ง (อยู่นอก data/ โดยตั้งใจ)
│                                   Layer 06.8: ไฟล์เดียวกันนี้กลายเป็น "สัญญาณคุณค่า" ด้วย — ไม่ใช่แค่ใบอนุญาตให้ถอดเส้นทาง
│                                   (คำประกาศของแต่ละรอบอยู่ใน data/evolution.json ที่ field `adoption`)
├── evolution/deprecations.json     Layer 06.6: ทะเบียนเส้นทางที่ถูกถอดออกจาก regression sweep (ย้อนกลับได้)
└── evolution/backups/<evo-id>/
    ├── files/              Layer 06: โค้ดเวอร์ชัน "ก่อน" รอบนั้น (ใช้ย้อนกลับ)
    ├── proof.js            Layer 06: สำเนาไฟล์พิสูจน์ (เก็บไว้แม้รอบนั้นถูกตีตก)
    └── attempted/          Layer 06.5: โค้ดที่รอบนั้น "เขียนแล้วไม่ผ่าน" — ไม่ถูกลบทิ้ง
                            รอบถัดไปที่ชนกำแพงเดิมจะถูกสั่งให้เปิดอ่านก่อนลงมือ
```

## ทำไมชั้นที่ 6 ถึงเป็นบทสรุปของทั้งเครื่องมือ

Jobs บอกว่า "คุณเชื่อมจุดได้เฉพาะตอนมองย้อนกลับ" — v3 มองย้อนกลับแทนคุณ
v4 ถามคำถามที่หนักกว่านั้น: **จุดชุดสุดท้ายที่เครื่องมือนี้ยังไม่เคยเชื่อม คือตัวมันเอง**
เครื่องมือที่สร้างนวัตกรรมให้คนอื่นแต่พัฒนาตัวเองไม่ได้ ก็ยังเป็นแค่เครื่องมือ —
พอมันอ่านตัวเอง ตั้งชื่อกำแพงของตัวเองได้ และเขียนโค้ดข้ามกำแพงนั้นโดยพิสูจน์ได้
เพดานของมันก็เลิกเป็นค่าคงที่ กลายเป็นตัวแปรที่มันขยับเองได้

สามชั้นถัดมาแก้สมมติฐานที่เหลืออยู่ทีละข้อ:

- **06.5 — ความล้มเหลวเลิกเป็นของที่ต้องทิ้ง** ระบบที่ลืมว่าเมื่อวานพลาดตรงไหน จะลองสามครั้งเหมือนครั้งแรกทุกครั้ง
  แล้วสรุปว่ากำแพงนั้น "ทำไม่ได้" ทั้งที่ยังไม่เคยลองวิธีที่สอง · ตอนนี้กำแพงถูกปิดก็ต่อเมื่อระบบ *ลองวิธีเดิมซ้ำ* เท่านั้น
- **06.6 — ความคืบหน้าเลิกแปลว่าใหญ่ขึ้น** ด่านความต่างสั่งห้ามการลดขนาดตัวเองไปโดยไม่มีใครสังเกต
  ทุกความสำเร็จจึงดันเพดานบริบทของมันเองเข้ามาใกล้อีกก้าว · ตอนนี้มีรอบที่เดินอีกทิศและพิสูจน์ได้เท่ากัน
- **06.7 — ผลตรวจเลิกมาสายเกินไป** ระบบตรวจข้อสอบหลังผู้เขียนออกจากห้องไปแล้ว แล้วเก็บคำตอบไว้ให้คนที่จะเข้ามาพรุ่งนี้
  ทั้งที่ stdout ของด่านมีอยู่ครบตั้งแต่นาทีที่สาม · ตอนนี้ผลตรวจกลับเข้าเซสชันเดิมภายในรอบเดียวกัน — ด่านไม่ได้อ่อนลงแม้แต่ด่านเดียว แค่เลิกพูดกับห้องว่าง
- **07 — ความรู้เลิกเข้าทางเดียว** สมมติฐานที่ไม่มีใครตั้งคำถามคือ **"จุดคือประสบการณ์ของผู้ใช้"** มันจึงรอคุณพิมพ์
  ทั้งที่ Evidence Agent ค้นโลกจริงทุกครั้งแล้วทิ้งสิ่งที่เจอ · คลังยังเป็นของคุณ (ทุกจุดติดป้ายที่มา และยับยั้งได้ถาวร)
  แต่คุณไม่ต้องเป็นคนเดียวที่แบกความรู้เข้ามาอีกต่อไป

- **06.8 — "ผ่านทุกด่าน" เลิกเป็นคำตัดสินสุดท้าย** ทุกด่านที่ระบบมีตัดสินที่วินาทีที่โค้ดเขียนเสร็จ
  จึงพิสูจน์ได้แค่ว่า **"ทำได้"** ไม่มีด่านไหนถามว่า **"มีใครใช้"** ทั้งที่คำตอบนอนอยู่ใน `endpoint-usage.json`
  มาตั้งแต่ 06.6 — แต่ถูกใช้เป็นแค่ใบอนุญาตให้ถอดเส้นทาง ระบบจึงสะสมของที่ถูกเรียก 0 ครั้งได้ไม่จำกัด
  และนับทุกครั้งว่าเป็นความคืบหน้า · ตอนนี้ทุกรอบต้องประกาศเส้นทางที่มันจะถูกใช้ผ่าน แล้วอีกไม่กี่วัน
  บันทึกการใช้งานให้คะแนนมันด้วยสูตรเดียวกับที่ให้คะแนนไอเดียของคุณ — และคะแนนนั้นเปลี่ยนว่าจะสร้างอะไรต่อ
- **06.9 — เครื่องมือเชื่อมจุดเลิกลืมจุดของตัวเอง** ทั้งเครื่องมือนี้มีอยู่เพื่อทาบโครงสร้างข้ามโดเมน
  แต่ชั้นที่ตัดสินว่ามันจะกลายเป็นอะไร ได้รับแค่ตัวขอบเขต ประวัติความล้มเหลว และซอร์สโค้ดของตัวเอง — **ไม่มีจุดความรู้แม้แต่จุดเดียว**
  ส่วนชั้นที่ตั้งชื่อกำแพงได้เห็นคลังก็จริง แต่เห็นแค่ชื่อจุด: คินสึงิ ภูมิคุ้มกันแบบจดจำ ทฤษฎีแถวคอย เป็นได้แค่รายชื่อ ไม่เคยเป็นวัตถุดิบ
  · ไม่มีใครถอดมันออกด้วยความตั้งใจ — พรอมป์ตใหญ่จนน่ากลัวอยู่แล้ว การเติมอะไรเข้าไปดูเหมือนการทำร้ายตัวเอง
  และคำถามที่ไม่มีใครถามคือ *อะไรควรถูกตัดออกเพื่อให้ความรู้เข้ามาแทน* · ตอนนี้บล็อกความรู้มีเพดานของตัวเอง (จึงไม่โตตามคลัง)
  ระบบเอาเครื่องมือเชื่อมจุดมาเล็งกำแพงของตัวเองก่อนลงมือ และทุกรอบต้องประกาศว่าใช้หลักการจากจุดไหน —
  **คำประกาศนั้นถูกให้คะแนนด้วยผลของด่านทั้งหมด คลังความรู้จึงเลิกเป็นคลังที่ผิดไม่ได้**
- **08 — คลังเลิกโตทางเดียว** 06.6 หากำแพง "ความคืบหน้าแปลว่าใหญ่ขึ้น" เจอในฝั่งโค้ดแล้วทำลายมัน
  แต่กำแพงรูปทรงเดียวกันฝั่งความรู้ยืนอยู่ครบโดยไม่มีใครมอง: ทุกทางเข้าเพิ่มจุด ทางออกมีแค่นิ้วมือมนุษย์
  และ "การลืม" ของระบบเป็นแค่ป้ายสี — จุดที่ถูกทำเครื่องหมายว่าถูกลืม ยังถูกอ่านครบทุกตัวอักษรทุกรอบ
  · ตอนนี้พรอมป์ตมีเพดาน (ลืมเนื้อหาได้จริงโดยไม่ลืมการมีอยู่) และคลังยุบรวมตัวเองได้โดยพิสูจน์ได้ว่าไม่มีอะไรหาย

**คลังที่โตได้เฉพาะตอนเจ้าของว่าง คือคลังที่หยุดโตเป็นส่วนใหญ่ของเวลา —
และคลังที่โตได้อย่างเดียว คือคลังที่วันหนึ่งจะใหญ่เกินกว่าที่ตัวมันเองจะอ่านจบ**

**และระบบที่ตัดสินตัวเองเสร็จภายในวันเดียวกับที่เขียนโค้ด คือระบบที่พิสูจน์ได้เสมอว่า "ทำได้"
โดยไม่เคยต้องรู้เลยว่ามีใครใช้ — ความคืบหน้าที่วัดจากด้านผู้สร้างล้วน ๆ ก็แยกไม่ออกจากการสะสมของที่ไม่มีใครแตะ**
